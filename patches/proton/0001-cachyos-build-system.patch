Generated by scripts/extract-fork-patches.sh
Source: proton-cachyos cachyos-11.0-20260703-slr
Rebased onto: Proton bleeding-edge af99d229

diff --git a/.gitmodules b/.gitmodules
index 09fc03927a9d72e8f63ec9d51be1c1e88d5e87a0..d525c8a4688d724496b135236cbf6b7b6c471576 100644
--- a/.gitmodules
+++ b/.gitmodules
@@ -1,6 +1,33 @@
 [submodule "wine"]
 	path = wine
-	url = ../wine
+	url = https://github.com/CachyOS/wine-cachyos.git
+[submodule "meson"]
+	path = meson
+	url = https://github.com/mesonbuild/meson.git
+[submodule "nvidia-libs/dxvk-nvapi"]
+	path = nvidia-libs/dxvk-nvapi
+	url = https://github.com/SveSop/dxvk-nvapi.git
+	branch = experimental
+[submodule "nvidia-libs/nvcuda"]
+	path = nvidia-libs/nvcuda
+	url = https://github.com/SveSop/nvcuda.git
+[submodule "nvidia-libs/nvcuda32"]
+	path = nvidia-libs/nvcuda32
+	url = https://github.com/SveSop/nvcuda.git
+	branch = 32bit_only
+[submodule "nvidia-libs/nvenc"]
+	path = nvidia-libs/nvenc
+	url = https://github.com/SveSop/nvenc.git
+[submodule "nvidia-libs/nvenc32"]
+	path = nvidia-libs/nvenc32
+	url = https://github.com/SveSop/nvenc.git
+	branch = old-32-64_version
+[submodule "nvidia-libs/wine-nvml"]
+	path = nvidia-libs/wine-nvml
+	url = https://github.com/Saancreed/wine-nvml.git
+[submodule "nvidia-libs/wine-nvoptix"]
+	path = nvidia-libs/wine-nvoptix
+	url = https://github.com/SveSop/wine-nvoptix.git
 [submodule "dxvk"]
 	path = dxvk
 	url = https://github.com/doitsujin/dxvk.git
@@ -10,12 +37,45 @@
 [submodule "fonts/liberation-fonts"]
 	path = fonts/liberation-fonts
 	url = https://github.com/liberationfonts/liberation-fonts
+[submodule "steamrtdeps/libxml2"]
+	path = steamrtdeps/libxml2
+	url = https://github.com/GNOME/libxml2.git
+[submodule "steamrtdeps/libxkbcommon"]
+	path = steamrtdeps/libxkbcommon
+	url = https://github.com/xkbcommon/libxkbcommon.git
+[submodule "steamrtdeps/wayland-protocols"]
+	path = steamrtdeps/wayland-protocols
+	url = https://gitlab.freedesktop.org/wayland/wayland-protocols.git
+[submodule "steamrtdeps/libpcap"]
+	path = steamrtdeps/libpcap
+	url = https://github.com/the-tcpdump-group/libpcap.git
+[submodule "steamrtdeps/libwebp"]
+	path = steamrtdeps/libwebp
+	url = https://github.com/webmproject/libwebp.git
+[submodule "steamrtdeps/pipewire"]
+	path = steamrtdeps/pipewire
+	url = https://github.com/PipeWire/pipewire.git
+[submodule "steamrtdeps/xz"]
+	path = steamrtdeps/xz
+	url = https://github.com/tukaani-project/xz.git
+[submodule "steamrtdeps/speex"]
+	path = steamrtdeps/speex
+	url = https://github.com/xiph/speex.git
+[submodule "steamrtdeps/speexdsp"]
+	path = steamrtdeps/speexdsp
+	url = https://github.com/xiph/speexdsp.git
+[submodule "steamrtdeps/flac"]
+	path = steamrtdeps/flac
+	url = https://github.com/xiph/flac.git
 [submodule "gstreamer"]
 	path = gstreamer
 	url = https://github.com/GStreamer/gstreamer.git
 [submodule "gst-orc"]
 	path = gst-orc
 	url = https://github.com/GStreamer/orc.git
+[submodule "protonfixes"]
+	path = protonfixes
+	url = https://github.com/Open-Wine-Components/umu-protonfixes
 [submodule "vkd3d-proton"]
 	path = vkd3d-proton
 	url = https://github.com/HansKristian-Work/vkd3d-proton
@@ -75,3 +135,30 @@
 [submodule "libsoup"]
 	path = libsoup
 	url = https://github.com/GNOME/libsoup.git
+[submodule "extras/dxvk-sarek"]
+	path = extras/dxvk-sarek
+	url = https://github.com/pythonlover02/DXVK-Sarek.git
+[submodule "extras/d7vk"]
+	path = extras/d7vk
+	url = https://github.com/WinterSnowfall/d7vk.git
+[submodule "extras/dxvk-low-latency"]
+	path = extras/dxvk-low-latency
+	url = https://github.com/netborg-afps/dxvk-low-latency.git
+[submodule "extras/vkd3d-low-latency"]
+	path = extras/vkd3d-low-latency
+	url = https://github.com/netborg-afps/vkd3d-low-latency.git
+[submodule "extras/discord-rpc-bridge"]
+	path = extras/discord-rpc-bridge
+	url = https://github.com/enderice2/rpc-bridge.git
+[submodule "vklayers/pyroveil"]
+	path = vklayers/pyroveil
+	url = https://github.com/HansKristian-Work/pyroveil.git
+[submodule "vklayers/Vulkan-Utility-Libraries"]
+	path = vklayers/Vulkan-Utility-Libraries
+	url = https://github.com/KhronosGroup/Vulkan-Utility-Libraries.git
+[submodule "vklayers/low_latency_layer"]
+	path = vklayers/low_latency_layer
+	url = https://github.com/Korthos-Software/low_latency_layer.git
+[submodule "vklayers/vkbasalt"]
+	path = vklayers/vkbasalt
+	url = https://github.com/DadSchoorse/vkBasalt.git
diff --git a/Makefile.in b/Makefile.in
index 08f6dce5c754d3f154285c1b09cc6655e2c21dd7..fdddc138afa2c63d079323a2b389e03a2d4d2f72 100644
--- a/Makefile.in
+++ b/Makefile.in
@@ -51,13 +51,20 @@ DIST_VERSION := $(DST_BASE)/version
 DEPLOY_DIR := ./deploy
 REDIST_DIR := ./redist
 
-i386_CFLAGS    := -mstackrealign -march=nocona -mtune=core-avx2 -mfpmath=sse
-x86_64_CFLAGS  := -mcmodel=small -march=nocona -mtune=core-avx2 -mfpmath=sse
-aarch64_CFLAGS := -march=armv8.2-a -mtune=cortex-x3
-arm64ec_CFLAGS := -march=armv8.2-a -mtune=cortex-x3
-
-CFLAGS  = -O2 -fwrapv -fno-strict-aliasing
-CFLAGS += -ggdb -ffunction-sections -fdata-sections -fno-omit-frame-pointer
+HOST_CFLAGS    ?= -O2 -march=nocona -mtune=core-avx2
+CCOS_CFLAGS    := $(HOST_CFLAGS) -pipe
+i386_CFLAGS    := -mstackrealign $(CCOS_CFLAGS) -mfpmath=sse
+x86_64_CFLAGS  := -mcmodel=small $(CCOS_CFLAGS) -mfpmath=sse
+HOST_RUSTFLAGS   ?= -Copt-level=3 -Ctarget-cpu=nocona
+i386_RUSTFLAGS   := $(HOST_RUSTFLAGS)
+x86_64_RUSTFLAGS := $(HOST_RUSTFLAGS)
+aarch64_CFLAGS := -O2 -march=armv8.2-a -mtune=cortex-x3
+arm64ec_CFLAGS := -O2 -march=armv8.2-a -mtune=cortex-x3
+aarch64_RUSTFLAGS := -Copt-level=3 -Ctarget-cpu=armv8.2-a
+arm64ec_RUSTFLAGS := -Copt-level=3 -Ctarget-cpu=armv8.2-a
+
+CFLAGS  = -fwrapv -fno-strict-aliasing
+CFLAGS += -ffunction-sections -fdata-sections -fno-omit-frame-pointer
 CFLAGS += -ffile-prefix-map=$(CCACHE_BASEDIR)=.
 
 ifneq ($(SUPPRESS_WARNINGS),)
@@ -75,7 +82,10 @@ ARCHS := i386-windows x86_64-windows
 TARGET_ARCH ?= x86_64
 
 ifeq ($(TARGET_ARCH),x86_64)
-	ARCHS += i386-unix x86_64-unix
+	ARCHS += x86_64-unix
+ifneq ($(ENABLE_WOW64),1)
+	ARCHS += i386-unix
+endif
 else ifeq ($(TARGET_ARCH),arm64)
 	ARCHS += aarch64-unix aarch64-windows arm64ec-windows
 else
@@ -98,12 +108,20 @@ ifeq ($(shell cc --version|grep 'clang'),)
 	TOOLCHAIN_PREFIX :=
 	OBJCOPY_FLAGS  := -p
 	MINGW_TYPE := gcc
+	i386_CFLAGS += -mno-avx -mno-avx2 -mno-avx512f
+	i386_CFLAGS += -fvect-cost-model=cheap
+	x86_64_CFLAGS += -mno-avx -mno-avx2 -mno-avx512f
+	x86_64_CFLAGS += -fvect-cost-model=cheap
+	i386_SANITY_FLAGS := -mpreferred-stack-boundary=2
 else
 	# CLANG-specific flags
 	TOOLCHAIN_PREFIX := llvm-
 	OBJCOPY_FLAGS  :=
 	MINGW_TYPE := clang
+	i386_CFLAGS += -mno-avx -mno-avx2 -mno-avx512f
+	x86_64_CFLAGS += -mno-avx -mno-avx2 -mno-avx512f
 	# TODO: move this to a configure option
+	i386_SANITY_FLAGS := -mstack-alignment=4
 endif
 
 OBJCOPY := $(TOOLCHAIN_PREFIX)objcopy
@@ -112,10 +130,14 @@ ifneq ($(UNSTRIPPED_BUILD),)
     STRIP :=
     INSTALL_PROGRAM_FLAGS :=
     MESON_STRIP_ARG :=
+    CFLAGS += -ggdb
 else
     STRIP := $(TOOLCHAIN_PREFIX)strip
     INSTALL_PROGRAM_FLAGS := -s
     MESON_STRIP_ARG := --strip
+ifeq ($(shell cc --version|grep 'clang'),)
+    CFLAGS += -s
+endif
 endif
 
 # all, all-dist and dist are basically synonyms
@@ -140,6 +162,38 @@ include $(SRC)/make/rules-wine-tools.mk
 include $(SRC)/make/rules-cargo.mk
 
 
+##
+## patches
+##
+
+$(eval $(call rules-source,patches,$(SRCDIR)/patches))
+
+
+##
+## meson
+##
+
+$(eval $(call rules-source,meson,$(SRCDIR)/meson))
+
+
+##
+## steamrtdeps
+##
+
+ifneq ($(WITHOUT_STEAMRT_DEPENDS),1)
+include $(SRC)/steamrtdeps/build-libwebp.mk
+include $(SRC)/steamrtdeps/build-libxml2.mk
+include $(SRC)/steamrtdeps/build-libxkbcommon.mk
+include $(SRC)/steamrtdeps/build-wayland-protocols.mk
+include $(SRC)/steamrtdeps/build-libpcap.mk
+include $(SRC)/steamrtdeps/build-pipewire.mk
+include $(SRC)/steamrtdeps/build-xz.mk
+include $(SRC)/steamrtdeps/build-speex.mk
+include $(SRC)/steamrtdeps/build-speexdsp.mk
+include $(SRC)/steamrtdeps/build-flac.mk
+endif # WITHOUT_STEAMRT_DEPENDS
+
+
 ##
 ## dav1d
 ##
@@ -163,6 +217,11 @@ LIBSOUP_MESON_ARGS = \
 	-Dautobahn=disabled \
 	-Dtls_check=false
 
+LIBSOUP_MESON_ARGS += \
+	-Dntlm=disabled \
+	-Dintrospection=disabled \
+	-Dtests=false
+
 $(eval $(call rules-source,libsoup,$(SRCDIR)/libsoup))
 $(eval $(call rules-meson,libsoup,i386,unix))
 $(eval $(call rules-meson,libsoup,x86_64,unix))
@@ -176,6 +235,11 @@ $(eval $(call rules-meson,libsoup,aarch64,unix))
 GST_ORC_MESON_ARGS := \
 	-Dorc-test=disabled
 
+GST_ORC_MESON_ARGS += \
+	-Dexamples=disabled \
+	-Dtests=disabled \
+	-Dbenchmarks=disabled \
+
 $(eval $(call rules-source,gst_orc,$(SRCDIR)/gst-orc))
 $(eval $(call rules-meson,gst_orc,i386,unix))
 $(eval $(call rules-meson,gst_orc,x86_64,unix))
@@ -192,6 +256,16 @@ GSTREAMER_MESON_ARGS := \
 	-Dtools=disabled \
 	-Dbash-completion=disabled
 
+GSTREAMER_MESON_ARGS += \
+	-Dptp-helper-permissions=capabilities \
+	-Ddbghelp=disabled \
+	-Dintrospection=disabled \
+	-Dnls=disabled \
+	-Dexamples=disabled \
+	-Dtests=disabled \
+	-Ddoc=disabled \
+	-Dgst_parse=true \
+
 GSTREAMER_LDFLAGS = -lm
 GSTREAMER_DEPENDS = gst_orc
 
@@ -205,6 +279,11 @@ $(eval $(call rules-meson,gstreamer,aarch64,unix))
 ## graphene
 ##
 
+GRAPHENE_MESON_ARGS := \
+	-Dtests=false \
+	-Dintrospection=disabled \
+	-Dinstalled_tests=false
+
 $(eval $(call rules-source,graphene,$(SRCDIR)/graphene))
 $(eval $(call rules-meson,graphene,i386,unix))
 $(eval $(call rules-meson,graphene,x86_64,unix))
@@ -236,10 +315,22 @@ GST_BASE_MESON_ARGS := \
 	-Dgl_platform=glx,egl \
 	--wrap-mode nodownload \
 
+GST_BASE_MESON_ARGS += \
+	-Dintrospection=disabled \
+	-Dnls=disabled \
+	-Dexamples=disabled \
+	-Dtests=disabled \
+	-Ddoc=disabled \
+	-Dtools=disabled \
+	-Drawparse=enabled \
+	-Dgl_winsys=surfaceless,x11,wayland,egl \
+
 GST_BASE_LDFLAGS = -lm
 GST_BASE_DEPENDS = gst_orc graphene gstreamer
 GST_BASE_HOST_DEPENDS = gst_orc
 
+GST_BASE_DEPENDS += $(WAYLAND_PROTOCOLS_DEPENDENCY)
+
 $(eval $(call rules-source,gst_base,$(SRCDIR)/gstreamer/subprojects/gst-plugins-base))
 $(eval $(call rules-meson,gst_base,i386,unix))
 $(eval $(call rules-meson,gst_base,x86_64,unix))
@@ -271,9 +362,29 @@ GST_GOOD_MESON_ARGS := \
 	-Dsoup=enabled \
 	-Dorc=enabled
 
+GST_GOOD_MESON_ARGS += \
+	-Dnls=disabled \
+	-Dexamples=disabled \
+	-Dtests=disabled \
+	-Ddoc=disabled \
+	-Dflv=enabled \
+	-Dflx=enabled \
+	-Dflac=enabled \
+	-Dspeex=enabled \
+	-Dxingmux=enabled \
+	-Dy4m=enabled \
+	-Dadaptivedemux2=enabled \
+	-Djpeg=enabled \
+
 GST_GOOD_DEPENDS = gst_orc gstreamer gst_base libsoup
 GST_GOOD_HOST_DEPENDS = gst_orc
 
+GST_GOOD_DEPENDS += $(LIBXML2_DEPENDENCY)
+
+GST_GOOD_i386_DEPENDS += $(SPEEX_DEPENDENCY)
+GST_GOOD_i386_DEPENDS += $(SPEEXDSP_DEPENDENCY)
+GST_GOOD_i386_DEPENDS += $(FLAC_DEPENDENCY)
+
 $(eval $(call rules-source,gst_good,$(SRCDIR)/gstreamer/subprojects/gst-plugins-good/))
 $(eval $(call rules-meson,gst_good,i386,unix))
 $(eval $(call rules-meson,gst_good,x86_64,unix))
@@ -290,13 +401,139 @@ GST_BAD_MESON_ARGS := \
 	-Dmpegtsdemux=enabled \
 	-Daudiobuffersplit=enabled
 
+GST_BAD_MESON_ARGS += \
+	-Dintrospection=disabled \
+	-Dnls=disabled \
+	-Dexamples=disabled \
+	-Dtests=disabled \
+	-Ddoc=disabled \
+	-Daccurip=enabled \
+	-Dadpcmdec=enabled \
+	-Dadpcmenc=enabled \
+	-Daiff=enabled \
+	-Dasfmux=enabled \
+	-Daudiobuffersplit=enabled \
+	-Daudiofxbad=enabled \
+	-Daudiomixmatrix=enabled \
+	-Daudiolatency=enabled \
+	-Daudiovisualizers=enabled \
+	-Dautoconvert=enabled \
+	-Dbayer=enabled \
+	-Dcamerabin2=enabled \
+	-Dcodectimestamper=enabled \
+	-Dcoloreffects=enabled \
+	-Ddebugutils=enabled \
+	-Ddvbsubenc=enabled \
+	-Ddvbsuboverlay=enabled \
+	-Ddvdspu=enabled \
+	-Dfaceoverlay=enabled \
+	-Dfestival=enabled \
+	-Dfieldanalysis=enabled \
+	-Dfreeverb=enabled \
+	-Dfrei0r=enabled \
+	-Dgaudieffects=enabled \
+	-Dgdp=enabled \
+	-Dgeometrictransform=enabled \
+	-Did3tag=enabled \
+	-Dinter=enabled \
+	-Dinterlace=enabled \
+	-Divfparse=enabled \
+	-Divtc=enabled \
+	-Djp2kdecimator=enabled \
+	-Djpegformat=enabled \
+	-Dlibrfb=enabled \
+	-Dmidi=enabled \
+	-Dmpegdemux=enabled \
+	-Dmpegpsmux=enabled \
+	-Dmpegtsmux=enabled \
+	-Dmxf=enabled \
+	-Dnetsim=enabled \
+	-Donvif=enabled \
+	-Dpcapparse=enabled \
+	-Dpnm=enabled \
+	-Dproxy=enabled \
+	-Drawparse=enabled \
+	-Dremovesilence=enabled \
+	-Drist=enabled \
+	-Drtmp2=enabled \
+	-Drtp=enabled \
+	-Dsdp=enabled \
+	-Dsegmentclip=enabled \
+	-Dsiren=enabled \
+	-Dsmooth=enabled \
+	-Dspeed=enabled \
+	-Dsubenc=enabled \
+	-Dswitchbin=enabled \
+	-Dtimecode=enabled \
+	-Dtranscode=enabled \
+	-Dvideofilters=enabled \
+	-Dvideoframe_audiolevel=enabled \
+	-Dvideosignal=enabled \
+	-Dvmnc=enabled \
+	-Ddvb=enabled \
+	-Dgtk3=disabled \
+	-Dipcpipeline=enabled \
+	-Dkms=disabled \
+	-Dshm=enabled \
+	-Duvch264=enabled \
+	-Dv4l2codecs=enabled \
+	-Dbz2=enabled \
+	-Ddash=enabled \
+	-Dopus=enabled \
+	-Dsctp=enabled \
+	-Dsmoothstreaming=enabled \
+	-Dwebp=enabled \
+	-Dwayland=enabled \
+	-Dx11=enabled \
+	-Dorc=enabled
+
+GST_BAD_MESON_ARGS += \
+	-Daes=disabled \
+	-Dclosedcaption=disabled \
+	-Dcolormanagement=disabled \
+	-Ddtls=disabled \
+	-Dopenal=disabled \
+	-Dsndfile=disabled \
+	-Dttml=disabled
+
 GST_BAD_DEPENDS = gst_orc gstreamer gst_base
+GST_BAD_HOST_DEPENDS = gst_orc
+
+GST_BAD_DEPENDS += $(LIBXML2_DEPENDENCY)
+GST_BAD_DEPENDS += $(WAYLAND_PROTOCOLS_DEPENDENCY)
+
+GST_BAD_i386_DEPENDS += $(LIBWEBP_DEPENDENCY)
 
 $(eval $(call rules-source,gst_bad,$(SRCDIR)/gstreamer/subprojects/gst-plugins-bad/))
 $(eval $(call rules-meson,gst_bad,i386,unix))
 $(eval $(call rules-meson,gst_bad,x86_64,unix))
 $(eval $(call rules-meson,gst_bad,aarch64,unix))
 
+##
+## gst-plugins-ugly
+##
+
+GST_UGLY_MESON_ARGS := \
+	-Dauto_features=disabled \
+	-Dnls=disabled \
+	-Dtests=disabled \
+	-Ddoc=disabled \
+	-Dasfdemux=enabled \
+	-Ddvdlpcmdec=enabled \
+	-Ddvdsub=enabled \
+	-Drealmedia=enabled \
+	-Dorc=enabled
+
+GST_UGLY_DEPENDS = gst_orc gstreamer gst_base
+GST_UGLY_HOST_DEPENDS = gst_orc
+
+$(eval $(call rules-source,gst_ugly,$(SRCDIR)/gstreamer/subprojects/gst-plugins-ugly/))
+$(eval $(call rules-meson,gst_ugly,i386,unix))
+$(eval $(call rules-meson,gst_ugly,x86_64,unix))
+$(eval $(call rules-meson,gst_ugly,aarch64,unix))
+
+default_pfx: gst_ugly
+
 ##
 ## FFmpeg
 ##
@@ -310,38 +547,123 @@ FFMPEG_CONFIGURE_ARGS := \
 	--disable-inline-asm \
 	--enable-demuxer=matroska \
 
+FFMPEG_CONFIGURE_ARGS += \
+	--enable-decoders \
+	--enable-muxers \
+	--enable-demuxers \
+	--enable-hwaccels \
+	--enable-parsers \
+	--enable-protocols \
+	--enable-filters \
+	--enable-bsfs \
+	--enable-zlib \
+	--enable-libvpx \
+	--enable-libvorbis \
+	--enable-decoder=indeo5 \
+	--enable-decoder=adpcm_ms \
+	--enable-decoder=mpegvideo \
+	--enable-decoder=mpeg1video \
+	--enable-decoder=mp2 \
+	--enable-decoder=mpeg4 \
+	--enable-decoder=msmpeg4v1 \
+	--enable-decoder=msmpeg4v2 \
+	--enable-decoder=msmpeg4v3 \
+	--enable-decoder=vc1 \
+	--enable-decoder=vc1image \
+	--enable-decoder=wmav1 \
+	--enable-decoder=wmav2 \
+	--enable-decoder=wmapro \
+	--enable-decoder=wmalossless \
+	--enable-decoder=xma1 \
+	--enable-decoder=xma2 \
+	--enable-decoder=wmv3image \
+	--enable-decoder=wmv3 \
+	--enable-decoder=wmv2 \
+	--enable-decoder=wmv1 \
+	--enable-decoder=aac \
+	--enable-decoder=h264 \
+	--enable-decoder=hevc \
+	--enable-decoder=gif \
+	--enable-decoder=vorbis \
+	--enable-decoder=mp3 \
+	--enable-decoder=flac \
+	--enable-decoder=alac \
+	--enable-decoder=pcm* \
+	--enable-decoder=apng \
+	--enable-decoder=libvpx_vp8 \
+	--enable-decoder=libvpx_vp9 \
+	--enable-encoder=aac \
+	--enable-encoder=h264_vaapi \
+	--enable-encoder=hevc_vaapi \
+	--enable-demuxer=aac \
+	--enable-demuxer=gif \
+	--enable-demuxer=ogg \
+	--enable-demuxer=mov \
+	--enable-demuxer=mp3 \
+	--enable-demuxer=flac \
+	--enable-demuxer=wav \
+	--enable-demuxer=flv \
+	--enable-demuxer=apng \
+	--enable-demuxer=mpegts \
+	--enable-demuxer=asf \
+	--enable-demuxer=rtsp \
+	--enable-demuxer=xwma \
+	--enable-muxer=asf \
+	--enable-muxer=asf_stream \
+	--enable-muxer=flv \
+	--enable-muxer=mp4 \
+	--enable-muxer=dash \
+	--enable-muxer=webm \
+	--enable-muxer=mpegts \
+	--enable-parser=mpeg4video \
+	--enable-parser=h264 \
+	--enable-parser=hevc \
+	--enable-parser=vc1 \
+	--enable-parser=mpegaudio \
+	--enable-filter=scale \
+	--enable-protocol=file \
+	--enable-protocol=rtp \
+	--enable-protocol=rtmp \
+	--enable-protocol=rtsp \
+
 FFMPEG_i386-unix_CONFIGURE_ARGS := \
 	--cross-prefix=i686-linux-gnu- \
 	--arch=x86 \
 	--target-os=linux
 
+FFMPEG_i386-unix_CONFIGURE_ARGS += \
+	--optflags="$(CFLAGS) $(i386_CFLAGS)"
+
 FFMPEG_x86_64-unix_CONFIGURE_ARGS := \
 	--cross-prefix=x86_64-linux-gnu- \
 	--arch=x86_64 \
 	--target-os=linux
 
+FFMPEG_x86_64-unix_CONFIGURE_ARGS += \
+	--optflags="$(CFLAGS) $(x86_64_CFLAGS)"
+
 FFMPEG_aarch64-unix_CONFIGURE_ARGS := \
 	--cross-prefix=aarch64-linux-gnu- \
 	--arch=aarch64 \
 	--target-os=linux
 
+FFMPEG_i386_DEPENDS += $(LIBWEBP_DEPENDENCY)
+FFMPEG_i386_DEPENDS += $(XZ_DEPENDENCY)
+
 $(eval $(call rules-source,ffmpeg,$(SRCDIR)/ffmpeg))
 $(eval $(call rules-configure,ffmpeg,i386,unix))
 $(eval $(call rules-configure,ffmpeg,x86_64,unix))
 $(eval $(call rules-configure,ffmpeg,aarch64,unix))
 
-## Only use ffmpeg to build gst-libav; we don't ship it.
-$(OBJ)/.ffmpeg-i386-dist:
-	touch $@
-$(OBJ)/.ffmpeg-x86_64-dist:
-	touch $@
-$(OBJ)/.ffmpeg-aarch64-dist:
-	touch $@
 
 ##
 ## gst-libav
 ##
 
+GST_LIBAV_MESON_ARGS := \
+    -Dtests=disabled \
+    -Ddoc=disabled \
+
 GST_LIBAV_DEPENDS = gst_orc gstreamer gst_base ffmpeg
 
 $(eval $(call rules-source,gst_libav,$(SRCDIR)/gstreamer/subprojects/gst-libav))
@@ -411,6 +733,10 @@ $(eval $(call rules-cmake,glslang,i386,unix))
 $(eval $(call rules-cmake,glslang,x86_64,unix))
 $(eval $(call rules-cmake,glslang,aarch64,unix))
 
+$(OBJ)/.glslang-post-source: patches-source
+	$(foreach p,$(shell find $(PATCHES_SRC)/glslang/ -name "*.patch" | sort),patch -d $(GLSLANG_SRC) -Np1 -i $(p) &&) true
+	touch $@
+
 
 ##
 ## lsteamclient
@@ -442,11 +768,17 @@ $(eval $(call rules-cmake,openxr,aarch64,unix))
 ## Note 32-bit is not supported by SteamVR, so we don't build it.
 ##
 
-WINEOPENXR_LDFLAGS = -lopenxr_loader
+WINEOPENXR_LDFLAGS = -lopenxr_loader -lvulkan
 WINEOPENXR_DEPENDS = wine openxr
 
 WINEOPENXR_aarch64_PE_ARCHS = arm64ec x86_64
 
+ifneq ($(findstring i386,$(unix_ARCHS)),)
+WINEOPENXR_CFLAGS = -DWINE_NO_LONG_TYPES
+else
+WINEOPENXR_CFLAGS = -Wno-format -Wno-pointer-to-int-cast
+endif
+
 $(eval $(call rules-source,wineopenxr,$(SRCDIR)/wineopenxr))
 $(eval $(call rules-makedep,wineopenxr,x86_64))
 $(eval $(call rules-makedep,wineopenxr,aarch64))
@@ -458,6 +790,20 @@ $(WINEOPENXR64_JSON): $(WINEOPENXR_SRC)/wineopenxr64.json
 	cp -a $< $@
 
 
+##
+## umu.exe
+##
+
+UMUEXE_DEPENDS = wine
+
+$(eval $(call rules-source,umuexe,$(SRCDIR)/umu_helper))
+$(eval $(call rules-makedep,umuexe,i386))
+$(eval $(call rules-makedep,umuexe,x86_64))
+$(eval $(call rules-makedep,umuexe,aarch64))
+
+default_pfx: umuexe
+
+
 ##
 ## steam.exe
 ##
@@ -470,6 +816,8 @@ $(eval $(call rules-makedep,steamexe,x86_64))
 $(eval $(call rules-makedep,steamexe,aarch64))
 
 
+ifneq ($(WITHOUT_TTS),1)
+
 ##
 ## piper
 ##
@@ -521,6 +869,11 @@ OPENFST_SOURCE_ARGS = \
   --exclude missing \
   --exclude test-driver \
 
+OPENFST_i386_CFLAGS := -mno-bmi2
+OPENFST_i386_CXXFLAGS := -mno-bmi2
+OPENFST_x86_64_CFLAGS := -mno-bmi2
+OPENFST_x86_64_CXXFLAGS := -mno-bmi2
+
 OPENFST_CONFIGURE_ARGS = \
   --enable-silent-rules \
   --enable-static \
@@ -566,9 +919,11 @@ $(eval $(call rules-cmake,kaldi,aarch64,unix))
 # the same source directory.
 #
 # The below assures that only a single configure/build step can run at a time.
+ifneq ($(findstring i386,$(unix_ARCHS)),)
 $(OBJ)/.kaldi-x86_64-configure: $(OBJ)/.kaldi-i386-configure
 $(OBJ)/.kaldi-i386-build: $(OBJ)/.kaldi-x86_64-configure
 $(OBJ)/.kaldi-i386-build: $(OBJ)/.kaldi-x86_64-build
+endif
 
 ##
 ## vosk
@@ -584,6 +939,8 @@ $(eval $(call rules-cmake,vosk,i386,unix))
 $(eval $(call rules-cmake,vosk,x86_64,unix))
 $(eval $(call rules-cmake,vosk,aarch64,unix))
 
+endif # WITHOUT_TTS
+
 
 ##
 ## wine
@@ -611,11 +968,24 @@ WINE_SOURCE_ARGS = \
   --exclude server/request_handlers.h \
   --exclude server/request_trace.h \
 
+WINE_CFLAGS += \
+  -Wno-stringop-overflow \
+  -Wno-incompatible-pointer-types \
+
 WINE_AUTOCONF_ARGS = \
-  --enable-werror \
   --with-mingw=$(MINGW_TYPE) \
+  --enable-build-id \
   --disable-tests
 
+WINE_AUTOCONF_ARGS += \
+  --with-x \
+  --with-pipewire \
+  --with-wayland \
+  --with-gstreamer \
+  --with-ffmpeg \
+  --with-pcap \
+  --without-oss \
+
 WINE_x86_64_AUTOCONF_ARGS = \
   --enable-archs=$(call list-join,$(comma),x86_64 $(filter-out x86_64 $(unix_ARCHS),$(windows_ARCHS))) \
   --enable-win64 \
@@ -623,17 +993,34 @@ WINE_x86_64_AUTOCONF_ARGS = \
 WINE_aarch64_AUTOCONF_ARGS = \
   --enable-archs=$(call list-join,$(comma),arm64ec aarch64 $(filter-out arm64ec aarch64 $(unix_ARCHS),$(windows_ARCHS)))
 
+ifeq ($(shell cc --version|grep 'clang'),)
+ifneq ($(findstring i386,$(unix_ARCHS)),)
 WINE_i386_AUTOCONF_ARGS += \
   VKD3D_PE_CFLAGS="-I$(VULKAN_HEADERS_i386_DST)/include -I$(VKD3D_i386_DST)/include/vkd3d" \
   VKD3D_PE_LIBS="-L$(VKD3D_i386_LIBDIR)/vkd3d/i386-windows -l:libvkd3d-1.dll -l:libvkd3d-shader-1.dll -l:libvkd3d-utils-1.dll"
 WINE_x86_64_AUTOCONF_ARGS += \
   VKD3D_PE_CFLAGS="-I$(VULKAN_HEADERS_x86_64_DST)/include -I$(VKD3D_x86_64_DST)/include/vkd3d" \
   VKD3D_PE_LIBS="-L$(VKD3D_x86_64_LIBDIR)/vkd3d/x86_64-windows -l:libvkd3d-1.dll -l:libvkd3d-shader-1.dll -l:libvkd3d-utils-1.dll"
+WINE_VKD3D_DEPENDENCY = vkd3d
+endif
+endif
 
+ifneq ($(WITHOUT_TTS),1)
 WINE_DEPENDS = gst_orc gstreamer gst_base ffmpeg openfst kaldi vosk
-WINE_i386_DEPENDS = vkd3d
-WINE_x86_64_DEPENDS = piper vkd3d
+WINE_i386_DEPENDS = $(WINE_VKD3D_DEPENDENCY)
+WINE_x86_64_DEPENDS = piper $(WINE_VKD3D_DEPENDENCY)
 WINE_aarch64_DEPENDS = piper
+else
+WINE_DEPENDS = gst_orc gstreamer gst_base ffmpeg
+WINE_i386_DEPENDS = $(WINE_VKD3D_DEPENDENCY)
+WINE_x86_64_DEPENDS = $(WINE_VKD3D_DEPENDENCY)
+endif
+
+WINE_DEPENDS += $(LIBXML2_DEPENDENCY)
+WINE_DEPENDS += $(LIBXKBCOMMON_DEPENDENCY)
+WINE_DEPENDS += $(LIBPCAP_DEPENDENCY)
+
+WINE_i386_DEPENDS += $(PIPEWIRE_DEPENDENCY)
 
 WINE_i386_LIBDIR = $(WINE_i386_DST)/lib
 WINE_x86_64_LIBDIR = $(WINE_x86_64_DST)/lib
@@ -650,7 +1037,8 @@ $(eval $(call rules-autoconf,wine,aarch64,unix))
 
 $(WINE_SRC)/configure: $(WINE_ORIGIN)/aclocal.m4
 
-$(OBJ)/.wine-post-source:
+$(OBJ)/.wine-post-source: patches-source
+	$(foreach p,$(shell find $(PATCHES_SRC)/wine/ -name "*.patch" | sort),patch -d $(WINE_SRC) -Np1 -i $(p) &&) true
 	cd $(WINE_SRC) && XDG_CACHE_HOME=$(OBJ) dlls/winevulkan/make_vulkan -x vk.xml -X video.xml
 	-cd $(WINE_SRC) && tools/make_specfiles
 	touch $@
@@ -706,17 +1094,24 @@ DXVK_SOURCE_ARGS = \
 DXVK_arm64ec_CFLAGS = -marm64x
 DXVK_arm64ec_LDFLAGS = -marm64x
 
+DXVK_MESON_ARGS = -Db_ndebug=true --force-fallback-for=libdisplay-info
 DXVK_i386_MESON_ARGS = --bindir=$(DXVK_i386_DST)/lib/wine/dxvk/i386-windows
 DXVK_x86_64_MESON_ARGS = --bindir=$(DXVK_x86_64_DST)/lib/wine/dxvk/x86_64-windows
 DXVK_arm64ec_MESON_ARGS = --bindir=$(DXVK_arm64ec_DST)/lib/wine/dxvk/aarch64-windows
 DXVK_HOST_DEPENDS = glslang
 
+DXVK_i386_CFLAGS = -O3 -mno-avx $(i386_SANITY_FLAGS)
+DXVK_x86_64_CFLAGS = -O3 -mno-avx
+DXVK_CPPFLAGS = -msse -msse2
+DXVK_LDFLAGS = -static -static-libgcc -static-libstdc++
+
 $(eval $(call rules-source,dxvk,$(SRCDIR)/dxvk))
 $(eval $(call rules-meson,dxvk,i386,windows))
 $(eval $(call rules-meson,dxvk,x86_64,windows))
 $(eval $(call rules-meson,dxvk,arm64ec,windows))
 
-$(OBJ)/.dxvk-post-source:
+$(OBJ)/.dxvk-post-source: patches-source
+	$(foreach p,$(shell find $(PATCHES_SRC)/dxvk/ -name "*.patch" | sort),patch -d $(DXVK_SRC) -Np1 -i $(p) &&) true
 	sed -re 's#@VCS_TAG@#$(shell git -C $(SRCDIR)/dxvk describe --always --abbrev=15 --dirty=0 || echo deadbeef)#' \
 	    $(SRCDIR)/dxvk/version.h.in > $(DXVK_SRC)/version.h.in
 	mkdir -p $(DST_LIBDIR)/wine/dxvk
@@ -729,10 +1124,16 @@ $(OBJ)/.dxvk-post-source:
 ## dxvk-nvapi
 ##
 
+DXVK_NVAPI_MESON_ARGS = -Db_ndebug=true
 DXVK_NVAPI_i386_MESON_ARGS = --bindir=$(DXVK_NVAPI_i386_DST)/lib/wine/nvapi/i386-windows
 DXVK_NVAPI_x86_64_MESON_ARGS = --bindir=$(DXVK_NVAPI_x86_64_DST)/lib/wine/nvapi/x86_64-windows
 DXVK_NVAPI_arm64ec_MESON_ARGS = --bindir=$(DXVK_NVAPI_arm64ec_DST)/lib/wine/nvapi/aarch64-windows
 
+DXVK_NVAPI_i386_CFLAGS = -O3 $(i386_SANITY_FLAGS)
+DXVK_NVAPI_x86_64_CFLAGS = -O3
+DXVK_NVAPI_CPPFLAGS = -msse -msse2
+DXVK_NVAPI_LDFLAGS = -static -static-libgcc -static-libstdc++
+
 $(eval $(call rules-source,dxvk-nvapi,$(SRCDIR)/dxvk-nvapi))
 $(eval $(call rules-meson,dxvk-nvapi,i386,windows))
 $(eval $(call rules-meson,dxvk-nvapi,x86_64,windows))
@@ -775,6 +1176,9 @@ VKD3D_CFLAGS = -fno-lto
 VKD3D_LDFLAGS = -static-libgcc $(CROSSLDFLAGS)
 VKD3D_DEPENDS = vulkan-headers spirv-headers
 
+VKD3D_i386_CFLAGS = -O3 $(i386_SANITY_FLAGS)
+VKD3D_x86_64_CFLAGS = -O3
+
 VKD3D_i386_LIBDIR = $(VKD3D_i386_DST)/lib
 VKD3D_x86_64_LIBDIR = $(VKD3D_x86_64_DST)/lib
 
@@ -808,6 +1212,8 @@ $(OBJ)/.vkd3d-i386-post-build: $(OBJ)/.wine-$(HOST_ARCH)-tools
 	cp $(VKD3D_i386_DST)/bin/libvkd3d-utils-1.dll $(VKD3D_i386_LIBDIR)/vkd3d/i386-windows
 	touch $@
 
+default_pfx: vkd3d
+
 
 ##
 ## vkd3d-proton
@@ -822,6 +1228,7 @@ VKD3D_PROTON_SOURCE_ARGS = \
   --exclude vkd3d_build.h.in \
   --exclude vkd3d_version.h.in \
 
+VKD3D_PROTON_MESON_ARGS = -Db_ndebug=true -Denable_extended_emulation=true
 VKD3D_PROTON_i386_MESON_ARGS = --bindir=$(VKD3D_PROTON_i386_DST)/lib/wine/vkd3d-proton/i386-windows
 VKD3D_PROTON_x86_64_MESON_ARGS = --bindir=$(VKD3D_PROTON_x86_64_DST)/lib/wine/vkd3d-proton/x86_64-windows
 VKD3D_PROTON_arm64ec_MESON_ARGS = --bindir=$(VKD3D_PROTON_arm64ec_DST)/lib/wine/vkd3d-proton/aarch64-windows
@@ -834,6 +1241,11 @@ ifneq ($(UNSTRIPPED_BUILD),)
 	VKD3D_PROTON_MESON_ARGS = -Denable_trace=true
 endif
 
+VKD3D_PROTON_i386_CFLAGS = -O3 $(i386_SANITY_FLAGS)
+VKD3D_PROTON_x86_64_CFLAGS = -O3
+VKD3D_PROTON_CPPFLAGS = -msse -msse2
+VKD3D_PROTON_LDFLAGS = -static -static-libgcc -static-libstdc++
+
 $(eval $(call rules-source,vkd3d-proton,$(SRCDIR)/vkd3d-proton))
 $(eval $(call rules-meson,vkd3d-proton,i386,windows))
 $(eval $(call rules-meson,vkd3d-proton,x86_64,windows))
@@ -850,6 +1262,44 @@ $(OBJ)/.vkd3d-proton-post-source:
 	touch $@
 
 
+##
+## extras
+##
+
+ifneq ($(WITHOUT_EXTRAS),1)
+include $(SRC)/extras/build-dxvk-sarek.mk
+include $(SRC)/extras/build-d7vk.mk
+include $(SRC)/extras/build-dxvk-low-latency.mk
+include $(SRC)/extras/build-vkd3d-low-latency.mk
+include $(SRC)/extras/build-discord-rpc-bridge.mk
+endif # WITHOUT_EXTRAS
+
+
+##
+## nvidia-libs
+##
+ifneq ($(WITHOUT_NVIDIA_LIBS),1)
+include $(SRC)/nvidia-libs/build-dxvk-nvapi.mk
+include $(SRC)/make/rules-nvidia-libs.mk
+include $(SRC)/nvidia-libs/build-nvcuda.mk
+include $(SRC)/nvidia-libs/build-nvenc.mk
+include $(SRC)/nvidia-libs/build-wine-nvml.mk
+include $(SRC)/nvidia-libs/build-wine-nvoptix.mk
+endif # WITHOUT_NVIDIA_LIBS
+
+
+##
+## vklayers
+##
+
+ifneq ($(WITHOUT_VKLAYERS),1)
+include $(SRC)/vklayers/build-dxvk-nvapi-vkreflex-layer.mk
+include $(SRC)/vklayers/build-pyroveil.mk
+include $(SRC)/vklayers/build-low_latency_layer.mk
+include $(SRC)/vklayers/build-vkbasalt.mk
+endif # WITHOUT_VKLAYERS
+
+
 ##
 ## FEX
 ##
@@ -1212,6 +1662,11 @@ $(DIST_FONTS): fonts
 
 all-dist: $(DIST_FONTS)
 
+fonts-distclean::
+	rm -rf $(FONTS_OBJ)
+
+distclean: fonts-distclean
+
 
 ##
 ## Gecko
@@ -1246,6 +1701,11 @@ $(OBJ)/.gecko-x86_64-dist: $(SRC)/contrib/$(GECKO_x86_64_TARBALL)
 
 all-dist: $(OBJ)/.gecko-i386-dist $(OBJ)/.gecko-x86_64-dist
 
+gecko-distclean::
+	rm -rf $(OBJ)/.gecko-{i386,x86_64}-dist
+
+distclean: gecko-distclean
+
 
 ##
 ## wine-mono
@@ -1270,6 +1730,11 @@ $(DIST_WINEMONO): $(SRC)/contrib/$(WINEMONO_TARBALL)
 
 all-dist: $(DIST_WINEMONO)
 
+mono-distclean::
+	rm -rf $(OBJ)/.wine-mono-dist
+
+distclean: mono-distclean
+
 
 ##
 ## xalia
@@ -1296,6 +1761,10 @@ $(DIST_XALIA): $(SRC)/contrib/$(XALIA_ZIP) $(SRC)/xalia-fixups.gudl
 
 all-dist: $(DIST_XALIA)
 
+xalia-distclean::
+	rm -rf $(OBJ)/.xalia-dist
+
+distclean: xalia-distclean
 
 ##
 ## ICU
@@ -1333,6 +1802,21 @@ ifneq ($(findstring aarch64,$(unix_ARCHS)),)
 all-dist: $(OBJ)/.icu-aarch64-dist
 endif
 
+icu-distclean::
+	rm -rf $(OBJ)/.icu-{x86_64,i386}-dist
+
+distclean: icu-distclean
+
+##
+## X11 locale
+##
+
+DIST_X11_LOCALE := $(DST_DIR)/share/X11
+$(DIST_X11_LOCALE): $(SRC)/locale
+	mkdir -p $@
+	cp -R $< $(DIST_X11_LOCALE)
+
+all-dist: $(DIST_X11_LOCALE)
 
 ##
 ## openvr
@@ -1426,6 +1910,47 @@ $(DIST_AV1_PATENTS): $(AV1_PATENTS)
 all-dist: $(DIST_LICENSE) $(DIST_OFL_LICENSE) $(DIST_AV1_PATENTS)
 
 
+##
+## protonfixes
+##
+
+$(eval $(call rules-source,protonfixes,$(SRCDIR)/protonfixes))
+$(eval $(call rules-common,protonfixes,x86_64,unix))
+$(eval $(call rules-common,protonfixes,aarch64,unix))
+
+$(OBJ)/.protonfixes-post-source: patches-source
+	$(foreach p,$(shell find $(PATCHES_SRC)/protonfixes/ -name "*.patch" | sort),patch -d $(PROTONFIXES_SRC) -Np1 -i $(p) &&) true
+	touch $@
+
+$(OBJ)/.protonfixes-x86_64-build:
+	cd $(PROTONFIXES_SRC) && \
+	env $(PROTONFIXES_x86_64_ENV) BUILD=$(PROTONFIXES_x86_64_OBJ) TARGET_ARCH=$(TARGET_ARCH) make
+	cd $(PROTONFIXES_SRC) && \
+	env $(PROTONFIXES_x86_64_ENV) BUILD=$(PROTONFIXES_x86_64_OBJ) DIST=$(PROTONFIXES_x86_64_DST) TARGET_ARCH=$(TARGET_ARCH) make install
+	touch $@
+
+$(OBJ)/.protonfixes-aarch64-build:
+	cd $(PROTONFIXES_SRC) && \
+	env $(PROTONFIXES_aarch64_ENV) BUILD=$(PROTONFIXES_aarch64_OBJ) TARGET_ARCH=$(TARGET_ARCH) make
+	cd $(PROTONFIXES_SRC) && \
+	env $(PROTONFIXES_aarch64_ENV) BUILD=$(PROTONFIXES_aarch64_OBJ) DIST=$(PROTONFIXES_aarch64_DST) TARGET_ARCH=$(TARGET_ARCH) make install
+	touch $@
+
+PROTONFIXES_TARGET := $(addprefix $(DST_BASE)/,protonfixes)
+
+$(OBJ)/.protonfixes-x86_64-dist:
+	mkdir -p $(PROTONFIXES_TARGET)
+	rsync --delete -arx $(PROTONFIXES_x86_64_DST)/ $(PROTONFIXES_TARGET)
+	touch $@
+
+$(OBJ)/.protonfixes-aarch64-dist:
+	mkdir -p $(PROTONFIXES_TARGET)
+	rsync --delete -arx $(PROTONFIXES_aarch64_DST)/ $(PROTONFIXES_TARGET)
+	touch $@
+
+all-dist: protonfixes
+
+
 ##
 ## proton(.py), filelock.py, etc.
 ##
@@ -1433,6 +1958,12 @@ all-dist: $(DIST_LICENSE) $(DIST_OFL_LICENSE) $(DIST_AV1_PATENTS)
 FILELOCK_TARGET := $(addprefix $(DST_BASE)/,filelock.py)
 $(FILELOCK_TARGET): $(addprefix $(SRCDIR)/,filelock.py)
 
+VULKAN_BINDINGS_TARGET := $(addprefix $(DST_BASE)/,vulkan.py)
+$(VULKAN_BINDINGS_TARGET): $(addprefix $(SRCDIR)/,vulkan.py)
+
+UTILITIES_TARGET := $(addprefix $(DST_BASE)/,utilities.py)
+$(UTILITIES_TARGET): $(addprefix $(SRCDIR)/,utilities.py)
+
 PROTON_PY_TARGET := $(addprefix $(DST_BASE)/,proton)
 $(PROTON_PY_TARGET): $(addprefix $(SRCDIR)/,proton)
 
@@ -1443,8 +1974,17 @@ USER_SETTINGS_PY_TARGET := $(addprefix $(DST_BASE)/,user_settings.sample.py)
 $(USER_SETTINGS_PY_TARGET): $(addprefix $(SRCDIR)/,user_settings.sample.py)
 
 DIST_COPY_TARGETS := $(FILELOCK_TARGET) $(PROTON_PY_TARGET) \
+                     $(VULKAN_BINDINGS_TARGET) $(UTILITIES_TARGET) \
                      $(PROTON37_TRACKED_FILES_TARGET) $(USER_SETTINGS_PY_TARGET)
 
+ifeq ($(ENABLE_WOW64),1)
+USER_SETTINGS_WOW64_PY_TARGET := $(addprefix $(DST_BASE)/,user_settings.py)
+$(USER_SETTINGS_WOW64_PY_TARGET): | $(DST_DIR)
+	echo "user_settings = {\"PROTON_USE_WOW64\": \"1\"}" > $@
+
+all-dist: $(USER_SETTINGS_WOW64_PY_TARGET)
+endif
+
 $(DIST_COPY_TARGETS): | $(DST_DIR)
 	cp -a $(SRCDIR)/$(notdir $@) $@
 
@@ -1476,6 +2016,22 @@ deploy: all
 redist: all
 	mkdir -p $(REDIST_DIR)
 	rsync --delete -arx $(DST_BASE)/ $(REDIST_DIR)
+	mv $(REDIST_DIR) $(BUILD_NAME)
+	XZ_OPT="-9 -T$(J)" tar -cvJf $(BUILD_NAME).tar.xz $(BUILD_NAME)
+	sha512sum $(BUILD_NAME).tar.xz > $(BUILD_NAME).sha512sum
+	@echo "Proton build available at $(BUILD_NAME).tar.xz"
+
+
+##
+## make distclean
+##
+
+dist-distclean::
+	rm -rf $(OBJ)/wine
+	rm -rf $(OBJ)/dist
+	rm -rf $(OBJ)/compile_commands
+
+distclean: dist-distclean
 
 
 ##
@@ -1526,7 +2082,7 @@ DOCKER_BASE = $(CONTAINER_ENGINE) run --rm -v $(SRC):$(SRC)$(CONTAINER_MOUNT_OPT
 
 .PHONY: dist deploy redist symstore-tarball
 .DEFAULT dist deploy redist symstore-tarball:
-	if [ "$(ENABLE_CCACHE)" -eq "1" ]; then mkdir -p $(CCACHE_DIR); fi
+	if [ "$(ENABLE_CCACHE)" = "1" ]; then mkdir -p $(CCACHE_DIR); fi
 	mkdir -p $(CARGO_HOME)
 	$(DOCKER_BASE) $(MAKE) -j$(J) $(filter -j%,$(MAKEFLAGS)) -f $(firstword $(MAKEFILE_LIST)) $(MFLAGS) $(MAKEOVERRIDES) CONTAINER=1 $@
 
diff --git a/configure.sh b/configure.sh
index 60b6df4367e1a8eb310fe1b015037c038030f55b..70c796575107e7e2c892f826571c9592ec960abc 100755
--- a/configure.sh
+++ b/configure.sh
@@ -187,6 +187,27 @@ function configure() {
     if [[ -n "$arg_enable_ccache" ]]; then
       echo "ENABLE_CCACHE := 1"
     fi
+    if [[ -n "$arg_enable_wow64" ]]; then
+      echo "ENABLE_WOW64 := 1"
+    fi
+    if [[ -n "$arg_without_tts" ]]; then
+      echo "WITHOUT_TTS := 1"
+    fi
+    if [[ -n "$arg_without_extras" ]]; then
+      echo "WITHOUT_EXTRAS := $arg_without_extras"
+    fi
+    if [[ -n "$arg_without_vklayers" ]]; then
+      echo "WITHOUT_VKLAYERS := $arg_without_vklayers"
+    fi
+    if [[ -n "$arg_without_nvidia_libs" ]]; then
+      echo "WITHOUT_NVIDIA_LIBS := 1"
+    fi
+    if [[ -n "$arg_without_steamrt_depends" ]]; then
+      echo "WITHOUT_STEAMRT_DEPENDS := 1"
+    fi
+
+    echo "HOST_CFLAGS := ${CFLAGS:--O2 -march=nocona -mtune=core-avx2}"
+    echo "HOST_RUSTFLAGS := ${RUSTFLAGS:--Copt-level=3 -Ctarget-cpu=nocona}"
 
     # Include base
     echo ""
@@ -208,6 +229,12 @@ arg_container_engine=""
 arg_docker_opts=""
 arg_relabel_volumes=""
 arg_enable_ccache=""
+arg_enable_wow64=""
+arg_without_tts=""
+arg_without_extras=""
+arg_without_vklayers=""
+arg_without_nvidia_libs=""
+arg_without_steamrt_depends=""
 arg_help=""
 invalid_args=""
 function parse_args() {
@@ -257,6 +284,22 @@ function parse_args() {
       arg_relabel_volumes="1"
     elif [[ $arg = --enable-ccache ]]; then
       arg_enable_ccache="1"
+    elif [[ $arg = --enable-wow64 ]]; then
+      arg_enable_wow64="1"
+    elif [[ $arg = --without-tts ]]; then
+      arg_without_tts="1"
+    elif [[ $arg = --without-extras ]]; then
+      if [[ $val = "all" ]]; then val=1; fi
+      arg_without_extras="$val"
+      val_used=1
+    elif [[ $arg = --without-vklayers ]]; then
+      if [[ $val = "all" ]]; then val=1; fi
+      arg_without_vklayers="$val"
+      val_used=1
+    elif [[ $arg = --without-nvidia-libs ]]; then
+      arg_without_nvidia_libs="1"
+    elif [[ $arg = --without-steamrt-depends ]]; then
+      arg_without_steamrt_depends="1"
     elif [[ $arg = --proton-sdk-image ]]; then
       val_used=1
       arg_protonsdk_image="$val"
@@ -314,6 +357,21 @@ usage() {
   "$1" ""
   "$1" "    --enable-ccache Mount \$CCACHE_DIR or \$HOME/.ccache inside of the container and use ccache for the build."
   "$1" ""
+  "$1" "    --enable-wow64 Build wine as wow64 only (excludes i386 unix libs from the build)"
+  "$1" ""
+  "$1" "    --without-extras=<list> Comma-separated list of extras to disable, or 'all' to disable everything."
+  "$1" "                            Values: dxvk-sarek | d7vk | dxvk-low-latency | vkd3d-low-latency | discord-rpc-bridge"
+  "$1" ""
+  "$1" "    --without-vklayers=<list> Comma-separated list of vulkan layers to disable, or 'all' to disable everything."
+  "$1" "                              Values: dxvk-nvapi-vkreflex-layer | low_latency_layer | pyroveil | vkbasalt"
+  "$1" ""
+  "$1" "    --without-tts Disables text-to-speech libraries (OpenFST, VOSK, Kaldi and Piper)"
+  "$1" ""
+  "$1" "    --without-nvidia-libs Disables alternative NVidia libraries (nvcuda, nvenc, nvml, nvoptix)"
+  "$1" ""
+  "$1" "    --without-steamrt-depends Disables bundling extra dependencies for SteamRT4, should be provided by the"
+  "$1" "                              package manager (for native builds)"
+  "$1" ""
   "$1" "  Steam Runtime"
   "$1" "    Proton builds that are to be installed & run under the steam client must be built with"
   "$1" "    the Steam Runtime SDK to ensure compatibility.  See README.md for more information."
diff --git a/default_pfx.py b/default_pfx.py
index 62f1c16fb30e9e9f232aeb21aa6b07671b3e4358..b1dfd36ab0e83f613399e9c68b64427a6128eaa8 100755
--- a/default_pfx.py
+++ b/default_pfx.py
@@ -127,6 +127,9 @@ def make_default_pfx(default_pfx_dir, dist_dir, arm64):
     local_env["WINEPREFIX"] = default_pfx_dir
     local_env["WINEDEBUG"] = "-all"
     local_env["WINEDLLPATH"] = libdir + "vkd3d"
+    local_env["WINEDLLOVERRIDES"] = "winex11.drv,winewayland.drv=d;winemenubuilder=d;"
+    local_env["WINEESYNC"] = "0"
+    local_env["WINEFSYNC"] = "0"
     runtime_args = []
 
 
diff --git a/extras/build-d7vk.mk b/extras/build-d7vk.mk
new file mode 100644
index 0000000000000000000000000000000000000000..3bce3145e4de156f8756e3ec510c7491b811d25c
--- /dev/null
+++ b/extras/build-d7vk.mk
@@ -0,0 +1,37 @@
+##
+## d7vk
+##
+
+ifeq ($(findstring d7vk,$(WITHOUT_EXTRAS)),)
+
+# wine builds DLLs with the same names, we need to differentiate the timestamps
+D7VK_i386_SOURCE_DATE_EPOCH := $(shell expr $(i386_SOURCE_DATE_EPOCH) - 1)
+
+D7VK_SOURCE_ARGS = \
+  --exclude version.h.in \
+
+D7VK_MESON_ARGS  = -Db_ndebug=true --force-fallback-for=libdisplay-info
+D7VK_MESON_ARGS += -Denable_dxgi=false -Denable_d3d8=false -Denable_d3d10=false -Denable_d3d11=false
+D7VK_i386_MESON_ARGS = --bindir=$(D7VK_i386_DST)/lib/wine/d7vk/i386-windows
+D7VK_HOST_DEPENDS = glslang
+
+D7VK_i386_CFLAGS = -O3 -mno-avx $(i386_SANITY_FLAGS)
+D7VK_CPPFLAGS = -msse -msse2
+D7VK_LDFLAGS = -static -static-libgcc -static-libstdc++
+
+$(eval $(call rules-source,d7vk,$(SRCDIR)/extras/d7vk))
+$(eval $(call rules-meson,d7vk,i386,windows))
+
+$(OBJ)/.d7vk-post-source:
+	sed -re 's#@VCS_TAG@#$(shell git -C $(SRCDIR)/extras/d7vk describe --always --abbrev=15 --dirty=0)#' \
+	    $(SRCDIR)/extras/d7vk/version.h.in > $(D7VK_SRC)/version.h.in
+	mkdir -p $(DST_LIBDIR)/wine/d7vk
+	rm -rf $(DST_LIBDIR)/wine/d7vk/version
+	echo "$(shell git -C $(SRCDIR) submodule status -- extras/d7vk)" > $(DST_LIBDIR)/wine/d7vk/version
+	touch $@
+
+default_pfx: d7vk
+
+endif # WITHOUT_EXTRAS
+
+
diff --git a/extras/build-discord-rpc-bridge.mk b/extras/build-discord-rpc-bridge.mk
new file mode 100644
index 0000000000000000000000000000000000000000..7e4cd94a5eeb8c805d462db9c7ff8fa5bf5c9bb8
--- /dev/null
+++ b/extras/build-discord-rpc-bridge.mk
@@ -0,0 +1,43 @@
+##
+## discord-rpc-bridge
+##
+
+ifeq ($(findstring discord-rpc-bridge,$(WITHOUT_EXTRAS)),)
+
+$(eval $(call rules-source,discord-rpc-bridge,$(SRCDIR)/extras/discord-rpc-bridge))
+$(eval $(call create-rules-common,discord-rpc-bridge,DISCORD_RPC_BRIDGE,x86_64,unix))
+$(eval $(call create-rules-common,discord-rpc-bridge,DISCORD_RPC_BRIDGE,aarch64,unix))
+
+$(OBJ)/.discord-rpc-bridge-post-source: patches-source
+	$(foreach p,$(shell find $(PATCHES_SRC)/extras/discord-rpc-bridge/ -name "*.patch" | sort),patch -d $(DISCORD_RPC_BRIDGE_SRC) -Np1 -i $(p) &&) true
+	touch $@
+
+$(OBJ)/.discord-rpc-bridge-x86_64-build:
+	@echo ":: building discord-rpc-bridge-x86_64..." >&2
+	rsync -arx "$(DISCORD_RPC_BRIDGE_SRC)/" "$(DISCORD_RPC_BRIDGE_x86_64_OBJ)/"
+	env $(DISCORD_RPC_BRIDGE_x86_64_ENV) \
+	    $(MAKE) -C "$(DISCORD_RPC_BRIDGE_x86_64_OBJ)" build
+	touch $@
+
+$(OBJ)/.discord-rpc-bridge-x86_64-post-build:
+	mkdir -p $(DISCORD_RPC_BRIDGE_x86_64_DST)/lib/wine/discord-rpc-bridge
+	$(call install-strip,$(DISCORD_RPC_BRIDGE_x86_64_OBJ)/build/bridge.exe,$(DISCORD_RPC_BRIDGE_x86_64_DST)/lib/wine/discord-rpc-bridge)
+	touch $@
+
+$(OBJ)/.discord-rpc-bridge-x86_64-dist:
+	mkdir -p $(DST_DIR)/lib/wine/discord-rpc-bridge
+	cp -a $(DISCORD_RPC_BRIDGE_x86_64_DST)/lib/wine/discord-rpc-bridge/bridge.exe $(DST_DIR)/lib/wine/discord-rpc-bridge/
+	touch $@
+
+$(OBJ)/.discord-rpc-bridge-aarch64-build:
+	touch $@
+
+$(OBJ)/.discord-rpc-bridge-aarch64-post-build:
+	touch $@
+
+$(OBJ)/.discord-rpc-bridge-aarch64-dist:
+	touch $@
+
+default_pfx: discord-rpc-bridge
+
+endif # WITHOUT_EXTRAS
diff --git a/extras/build-dxvk-low-latency.mk b/extras/build-dxvk-low-latency.mk
new file mode 100644
index 0000000000000000000000000000000000000000..8a94434ea2cbbd9a27f0b301697a5d3681dbc82f
--- /dev/null
+++ b/extras/build-dxvk-low-latency.mk
@@ -0,0 +1,42 @@
+##
+## dxvk-low-latency
+##
+
+ifeq ($(findstring dxvk-low-latency,$(WITHOUT_EXTRAS)),)
+
+# wine builds DLLs with the same names, we need to differentiate the timestamps
+DXVK_LOW_LATENCY_i386_SOURCE_DATE_EPOCH := $(shell expr $(i386_SOURCE_DATE_EPOCH) - 1)
+DXVK_LOW_LATENCY_x86_64_SOURCE_DATE_EPOCH := $(shell expr $(x86_64_SOURCE_DATE_EPOCH) - 1)
+DXVK_LOW_LATENCY_arm64ec_SOURCE_DATE_EPOCH := $(shell expr $(arm64ec_SOURCE_DATE_EPOCH) - 1)
+
+DXVK_LOW_LATENCY_SOURCE_ARGS = \
+  --exclude version.h.in \
+
+DXVK_LOW_LATENCY_MESON_ARGS = -Db_ndebug=true --force-fallback-for=libdisplay-info
+DXVK_LOW_LATENCY_i386_MESON_ARGS = --bindir=$(DXVK_LOW_LATENCY_i386_DST)/lib/wine/dxvk-low-latency/i386-windows
+DXVK_LOW_LATENCY_x86_64_MESON_ARGS = --bindir=$(DXVK_LOW_LATENCY_x86_64_DST)/lib/wine/dxvk-low-latency/x86_64-windows
+DXVK_LOW_LATENCY_arm64ec_MESON_ARGS = --bindir=$(DXVK_LOW_LATENCY_arm64ec_DST)/lib/wine/dxvk-low-latency/aarch64-windows
+DXVK_LOW_LATENCY_HOST_DEPENDS = glslang
+
+DXVK_LOW_LATENCY_i386_CFLAGS = -O3 -mno-avx $(i386_SANITY_FLAGS)
+DXVK_LOW_LATENCY_x86_64_CFLAGS = -O3 -mno-avx
+DXVK_LOW_LATENCY_CPPFLAGS = -msse -msse2
+DXVK_LOW_LATENCY_LDFLAGS = -static -static-libgcc -static-libstdc++
+
+$(eval $(call rules-source,dxvk-low-latency,$(SRCDIR)/extras/dxvk-low-latency))
+$(eval $(call rules-meson,dxvk-low-latency,i386,windows))
+$(eval $(call rules-meson,dxvk-low-latency,x86_64,windows))
+$(eval $(call rules-meson,dxvk-low-latency,arm64ec,windows))
+
+$(OBJ)/.dxvk-low-latency-post-source: patches-source
+	$(foreach p,$(shell find $(PATCHES_SRC)/extras/dxvk-low-latency/ -name "*.patch" | sort),patch -d $(DXVK_LOW_LATENCY_SRC) -Np1 -i $(p) &&) true
+	sed -re 's#@VCS_TAG@#$(shell git -C $(SRCDIR)/extras//dxvk-low-latency describe --always --abbrev=15 --dirty=0)#' \
+	    $(SRCDIR)/extras/dxvk-low-latency/version.h.in > $(DXVK_LOW_LATENCY_SRC)/version.h.in
+	mkdir -p $(DST_LIBDIR)/wine/dxvk-low-latency
+	rm -rf $(DST_LIBDIR)/wine/dxvk-low-latency/version
+	echo "$(shell git -C $(SRCDIR) submodule status -- extras/dxvk-low-latency)" > $(DST_LIBDIR)/wine/dxvk-low-latency/version
+	touch $@
+
+default_pfx: dxvk-low-latency
+
+endif # WITHOUT_EXTRAS
diff --git a/extras/build-dxvk-sarek.mk b/extras/build-dxvk-sarek.mk
new file mode 100644
index 0000000000000000000000000000000000000000..4ca282cd8178fae7a653fc814bcdaf308e25e3d3
--- /dev/null
+++ b/extras/build-dxvk-sarek.mk
@@ -0,0 +1,41 @@
+##
+## dxvk-sarek
+##
+
+ifeq ($(findstring dxvk-sarek,$(WITHOUT_EXTRAS)),)
+
+# wine builds DLLs with the same names, we need to differentiate the timestamps
+DXVK_SAREK_i386_SOURCE_DATE_EPOCH := $(shell expr $(i386_SOURCE_DATE_EPOCH) - 1)
+DXVK_SAREK_x86_64_SOURCE_DATE_EPOCH := $(shell expr $(x86_64_SOURCE_DATE_EPOCH) - 1)
+DXVK_SAREK_arm64ec_SOURCE_DATE_EPOCH := $(shell expr $(arm64ec_SOURCE_DATE_EPOCH) - 1)
+
+DXVK_SAREK_SOURCE_ARGS = \
+  --exclude version.h.in \
+
+DXVK_SAREK_MESON_ARGS = -Db_ndebug=true --force-fallback-for=libdisplay-info
+DXVK_SAREK_i386_MESON_ARGS = --bindir=$(DXVK_SAREK_i386_DST)/lib/wine/dxvk-sarek/i386-windows
+DXVK_SAREK_x86_64_MESON_ARGS = --bindir=$(DXVK_SAREK_x86_64_DST)/lib/wine/dxvk-sarek/x86_64-windows
+DXVK_SAREK_arm64ec_MESON_ARGS = --bindir=$(DXVK_SAREK_arm64ec_DST)/lib/wine/dxvk-sarek/aarch64-windows
+DXVK_SAREK_HOST_DEPENDS = glslang
+
+DXVK_SAREK_i386_CFLAGS = -O3 -mno-avx $(i386_SANITY_FLAGS)
+DXVK_SAREK_x86_64_CFLAGS = -O3 -mno-avx
+DXVK_SAREK_CPPFLAGS = -msse -msse2
+DXVK_SAREK_LDFLAGS = -static -static-libgcc -static-libstdc++
+
+$(eval $(call rules-source,dxvk-sarek,$(SRCDIR)/extras/dxvk-sarek))
+$(eval $(call rules-meson,dxvk-sarek,i386,windows))
+$(eval $(call rules-meson,dxvk-sarek,x86_64,windows))
+$(eval $(call rules-meson,dxvk-sarek,arm64ec,windows))
+
+$(OBJ)/.dxvk-sarek-post-source:
+	sed -re 's#@VCS_TAG@#$(shell git -C $(SRCDIR)/extras/dxvk-sarek describe --always --abbrev=15 --dirty=0)#' \
+	    $(SRCDIR)/extras/dxvk-sarek/version.h.in > $(DXVK_SAREK_SRC)/version.h.in
+	mkdir -p $(DST_LIBDIR)/wine/dxvk-sarek
+	rm -rf $(DST_LIBDIR)/wine/dxvk-sarek/version
+	echo "$(shell git -C $(SRCDIR) submodule status -- extras/dxvk-sarek)" > $(DST_LIBDIR)/wine/dxvk-sarek/version
+	touch $@
+
+default_pfx: dxvk-sarek
+
+endif # WITHOUT_EXTRAS
diff --git a/extras/build-vkd3d-low-latency.mk b/extras/build-vkd3d-low-latency.mk
new file mode 100644
index 0000000000000000000000000000000000000000..2b0d846c20680e5d7fd51e119e81f1519d8d3ee0
--- /dev/null
+++ b/extras/build-vkd3d-low-latency.mk
@@ -0,0 +1,49 @@
+##
+## vkd3d-low-latency
+##
+
+ifeq ($(findstring vkd3d-low-latency,$(WITHOUT_EXTRAS)),)
+
+# wine builds DLLs with the same names, we need to differentiate the timestamps
+VKD3D_LOW_LATENCY_i386_SOURCE_DATE_EPOCH := $(shell expr $(i386_SOURCE_DATE_EPOCH) - 2)
+VKD3D_LOW_LATENCY_x86_64_SOURCE_DATE_EPOCH := $(shell expr $(x86_64_SOURCE_DATE_EPOCH) - 2)
+VKD3D_LOW_LATENCY_arm64ec_SOURCE_DATE_EPOCH := $(shell expr $(arm64ec_SOURCE_DATE_EPOCH) - 2)
+
+VKD3D_LOW_LATENCY_SOURCE_ARGS = \
+  --exclude vkd3d_build.h.in \
+  --exclude vkd3d_version.h.in \
+
+VKD3D_LOW_LATENCY_MESON_ARGS = -Db_ndebug=true -Denable_extended_emulation=true
+VKD3D_LOW_LATENCY_i386_MESON_ARGS = --bindir=$(VKD3D_LOW_LATENCY_i386_DST)/lib/wine/vkd3d-low-latency/i386-windows
+VKD3D_LOW_LATENCY_x86_64_MESON_ARGS = --bindir=$(VKD3D_LOW_LATENCY_x86_64_DST)/lib/wine/vkd3d-low-latency/x86_64-windows
+VKD3D_LOW_LATENCY_arm64ec_MESON_ARGS = --bindir=$(VKD3D_LOW_LATENCY_arm64ec_DST)/lib/wine/vkd3d-low-latency/aarch64-windows
+VKD3D_LOW_LATENCY_HOST_DEPENDS = glslang
+
+ifneq ($(UNSTRIPPED_BUILD),)
+	VKD3D_LOW_LATENCY_MESON_ARGS = -Denable_trace=true
+endif
+
+VKD3D_LOW_LATENCY_i386_CFLAGS = -O3 $(i386_SANITY_FLAGS)
+VKD3D_LOW_LATENCY_x86_64_CFLAGS = -O3
+VKD3D_LOW_LATENCY_CPPFLAGS = -msse -msse2
+VKD3D_LOW_LATENCY_LDFLAGS = -static -static-libgcc -static-libstdc++
+
+$(eval $(call rules-source,vkd3d-low-latency,$(SRCDIR)/extras/vkd3d-low-latency))
+$(eval $(call rules-meson,vkd3d-low-latency,i386,windows))
+$(eval $(call rules-meson,vkd3d-low-latency,x86_64,windows))
+$(eval $(call rules-meson,vkd3d-low-latency,arm64ec,windows))
+
+$(OBJ)/.vkd3d-low-latency-post-source: patches-source
+	$(foreach p,$(shell find $(PATCHES_SRC)/extras/vkd3d-low-latency/ -name "*.patch" | sort),patch -d $(VKD3D_LOW_LATENCY_SRC) -Np1 -i $(p) &&) true
+	sed -re 's#@VCS_TAG@#$(shell git -C $(SRCDIR)/extras/vkd3d-low-latency describe --always --exclude=* --abbrev=15 --dirty=0)#' \
+	    $(SRCDIR)/extras/vkd3d-low-latency/vkd3d_build.h.in > $(VKD3D_LOW_LATENCY_SRC)/vkd3d_build.h.in
+	sed -re 's#@VCS_TAG@#$(shell git -C $(SRCDIR)/extras/vkd3d-low-latency describe --always --tags --dirty=+)#' \
+	    $(SRCDIR)/extras/vkd3d-low-latency/vkd3d_version.h.in > $(VKD3D_LOW_LATENCY_SRC)/vkd3d_version.h.in
+	mkdir -p $(DST_LIBDIR)/wine/vkd3d-low-latency
+	rm -rf $(DST_LIBDIR)/wine/vkd3d-low-latency/version
+	echo "$(shell git -C $(SRCDIR) submodule status -- extras/vkd3d-low-latency)" > $(DST_LIBDIR)/wine/vkd3d-low-latency/version
+	touch $@
+
+default_pfx: vkd3d-low-latency
+
+endif # WITHOUT_EXTRAS
diff --git a/extras/d7vk b/extras/d7vk
new file mode 160000
index 0000000000000000000000000000000000000000..d849777d4a18b8f697d00ddccf4e9adecbde1038
--- /dev/null
+++ b/extras/d7vk
@@ -0,0 +1 @@
+Subproject commit d849777d4a18b8f697d00ddccf4e9adecbde1038
diff --git a/extras/discord-rpc-bridge b/extras/discord-rpc-bridge
new file mode 160000
index 0000000000000000000000000000000000000000..6aa60772a5241c7bd1b8d38f026b2a5168e0f93c
--- /dev/null
+++ b/extras/discord-rpc-bridge
@@ -0,0 +1 @@
+Subproject commit 6aa60772a5241c7bd1b8d38f026b2a5168e0f93c
diff --git a/extras/dxvk-low-latency b/extras/dxvk-low-latency
new file mode 160000
index 0000000000000000000000000000000000000000..0ccdd368e9117fa6c67d4d6edc5319573c84ddd9
--- /dev/null
+++ b/extras/dxvk-low-latency
@@ -0,0 +1 @@
+Subproject commit 0ccdd368e9117fa6c67d4d6edc5319573c84ddd9
diff --git a/extras/dxvk-sarek b/extras/dxvk-sarek
new file mode 160000
index 0000000000000000000000000000000000000000..d5f8501be5b2f4c5066f053aaabb3e3096b87095
--- /dev/null
+++ b/extras/dxvk-sarek
@@ -0,0 +1 @@
+Subproject commit d5f8501be5b2f4c5066f053aaabb3e3096b87095
diff --git a/extras/vkd3d-low-latency b/extras/vkd3d-low-latency
new file mode 160000
index 0000000000000000000000000000000000000000..565d695093224e3aeeef190f430f8d6b223d235e
--- /dev/null
+++ b/extras/vkd3d-low-latency
@@ -0,0 +1 @@
+Subproject commit 565d695093224e3aeeef190f430f8d6b223d235e
diff --git a/make/rules-cargo.mk b/make/rules-cargo.mk
index 056a306b9ebc1234a1bea221a78734a556dea801..f1d1766b8e529b5b6664ad1efabcf8ec39a1dde9 100644
--- a/make/rules-cargo.mk
+++ b/make/rules-cargo.mk
@@ -31,6 +31,8 @@ i386-unix_CARGO_TARGET := i686-unknown-linux-gnu
 x86_64-unix_CARGO_TARGET := x86_64-unknown-linux-gnu
 aarch64-unix_CARGO_TARGET := aarch64-unknown-linux-gnu
 
+CARGO_RUSTFLAGS :=  $(foreach a,$(unix_ARCHS),CARGO_TARGET_$(call toupper,$($(a)-unix_CARGO_TARGET))_RUSTFLAGS="$($(a)_RUSTFLAGS)")
+
 i386-unix_CARGO_ARGS := --target $(i386-unix_CARGO_TARGET)
 x86_64-unix_CARGO_ARGS := --target $(x86_64-unix_CARGO_TARGET)
 aarch64-unix_CARGO_ARGS := --target $(aarch64-unix_CARGO_TARGET)
diff --git a/make/rules-common.mk b/make/rules-common.mk
index 0e3f44c640d8e14a92996cb88cebc56a6e7e8629..170548cb8008068bb3cb174648e029caa642b0f5 100644
--- a/make/rules-common.mk
+++ b/make/rules-common.mk
@@ -128,15 +128,21 @@ $(2)_$(3)_ENV = \
     PKG_CONFIG="$$($$(HOST_ARCH)-unix_TARGET)-pkg-config" \
     PATH="$$(call list-join,:,$$(foreach d,$$($(2)_$(3)_HOST_DEPS),$$($$(d)_$$(HOST_ARCH)_BINDIR)),,:):$$$$PATH" \
     LD_LIBRARY_PATH="$$(call list-join,:,$$(foreach d,$$($(2)_$(3)_HOST_DEPS),$$($$(d)_$$(HOST_ARCH)_LIBDIR)/$$($$(HOST_ARCH)-unix_LIBDIR)),,:)$$$$LD_LIBRARY_PATH" \
-    PKG_CONFIG_PATH="$$(call list-join,:,$$(foreach d,$$($(2)_$(3)_DEPS),$$($$(d)_$(3)_LIBDIR)/$$($(3)-$(4)_LIBDIR)/pkgconfig))" \
+    PKG_CONFIG_PATH="$$(call list-join,:,$$(foreach d,$$($(2)_$(3)_DEPS),$$($$(d)_$(3)_LIBDIR)/$$($(3)-$(4)_LIBDIR)/pkgconfig)):$$(call list-join,:,$$(foreach d,$$($(2)_$(3)_DEPS),$$($$(d)_$(3)_DST)/share/pkgconfig))" \
     PKG_CONFIG_LIBDIR="/usr/lib/$$($(3)-$(4)_LIBDIR)/pkgconfig:/usr/share/pkgconfig" \
     CMAKE_PREFIX_PATH="$$(call list-join,:,$$(foreach d,$$($(2)_$(3)_DEPS),$$($$(d)_$(3)_DST)))" \
-    CFLAGS="$$($(2)_$(3)_INCFLAGS) $$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS)" \
-    CPPFLAGS="$$($(2)_$(3)_INCFLAGS) $$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS)" \
-    CXXFLAGS="$$($(2)_$(3)_INCFLAGS) -std=c++17 $$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS)" \
+    CFLAGS="$$($(2)_$(3)_INCFLAGS) $$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS) $$($(2)_$(3)_CFLAGS)" \
+    CPPFLAGS="$$($(2)_$(3)_INCFLAGS) $$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS) $$($(2)_$(3)_CFLAGS)" \
+    CXXFLAGS="$$($(2)_$(3)_INCFLAGS) -std=c++17 $$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS) $$($(2)_$(3)_CFLAGS)" \
     LDFLAGS="$$($(2)_$(3)-$(4)_LIBFLAGS) $$($(2)_$(3)_LIBFLAGS) $$($(2)_LDFLAGS) $$($(3)_LDFLAGS) $$(LDFLAGS)" \
     SOURCE_DATE_EPOCH="$$($(2)_$(3)_SOURCE_DATE_EPOCH)" \
 
+$(2)_$(3)_ENV += \
+    WINEDEBUG="-all" \
+    WINEPREFIX="$$(OBJ)/pfx-wine-$(3)" WINEESYNC="0" WINEFSYNC="0" \
+    WINEDLLOVERRIDES="winex11.drv,winewayland.drv=d;winemenubuilder=d;" \
+    $$(CARGO_RUSTFLAGS) \
+
 ifeq ($(1),wine)
 
 # aarch64 always builds wine with clang directly so build tools do not need to be specified
@@ -171,6 +177,12 @@ $(2)_$(3)_ENV += \
 endif
 
 endif
+$(1)-$(3)-distclean::
+	rm -rf $$($(2)_$(3)_OBJ)
+	rm -rf $$($(2)_$(3)_DST)
+	rm  -f $$(OBJ)/.$(1)-$(3)-{configure,build,post-build,dist}
+
+distclean: $(1)-$(3)-distclean
 endef
 
 ifneq ($(UNSTRIPPED_BUILD),)
diff --git a/make/rules-meson.mk b/make/rules-meson.mk
index 2b55b398d18bbf77809180bb87b3ae50cd290b34..bb4cac2741cbeafcf0f6a766e52d9a763e36b9fc 100644
--- a/make/rules-meson.mk
+++ b/make/rules-meson.mk
@@ -22,8 +22,8 @@ pkgconfig = '$$$$PKG_CONFIG'
 
 [properties]
 needs_exe_wrapper = true
-c_args = [$$(call list-quote,$$($(2)_$(3)_INCFLAGS) $$($(2)_CFLAGS) $$($(3)_CFLAGS) $$($(2)_$(3)_CFLAGS) $$(CFLAGS))]
-cpp_args = [$$(call list-quote,$$($(2)_$(3)_INCFLAGS) $$($(2)_CFLAGS) $$($(3)_CFLAGS) $$($(2)_$(3)_CFLAGS) $$(CFLAGS))]
+c_args = [$$(call list-quote,$$($(2)_$(3)_INCFLAGS) $$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS) $$($(2)_$(3)_CFLAGS))]
+cpp_args = [$$(call list-quote,$$($(2)_$(3)_INCFLAGS) $$($(2)_CFLAGS) $$($(3)_CFLAGS) $$(CFLAGS) $$($(2)_$(3)_CFLAGS))]
 c_link_args = [$$(call list-quote,$$($(2)_$(3)-$(4)_LIBFLAGS) $$($(2)_$(3)_LIBFLAGS) $$($(2)_LDFLAGS) $$($(3)_LDFLAGS) $$($(2)_$(3)_CFLAGS) $$(LDFLAGS))]
 cpp_link_args = [$$(call list-quote,$$($(2)_$(3)-$(4)_LIBFLAGS) $$($(2)_$(3)_LIBFLAGS) $$($(2)_LDFLAGS) $$($(3)_LDFLAGS) $$($(2)_$(3)_CFLAGS) $$(LDFLAGS))]
 pkg_config_libdir = '$$$$PKG_CONFIG_LIBDIR'
@@ -40,14 +40,14 @@ export $(2)_$(3)_MESON_CROSS
 
 $$($(2)_SRC)/meson.build: | $$(OBJ)/.$(1)-post-source
 
-$$(OBJ)/.$(1)-$(3)-configure: $$($(2)_SRC)/meson.build
+$$(OBJ)/.$(1)-$(3)-configure: $$($(2)_SRC)/meson.build meson-source
 	@echo ":: configuring $(1)-$(3)..." >&2
 	rm -rf "$$($(2)_$(3)_OBJ)/meson-private/coredata.dat"
 
 	echo "$$$$$(2)_$(3)_MESON_CROSS" | env $$($(2)_$(3)_ENV) sh >"$$($(2)_$(3)_OBJ)/cross.txt"
 
 	env $$($(2)_$(3)_ENV) \
-	meson "$$($(2)_$(3)_OBJ)" "$$($(2)_SRC)" \
+	$$(OBJ)/src-meson/meson.py "$$($(2)_$(3)_OBJ)" "$$($(2)_SRC)" \
 	      --prefix="$$($(2)_$(3)_DST)" \
 	      --libdir="lib/$$($(3)-$(4)_LIBDIR)" \
 	      --buildtype=plain \
diff --git a/make/rules-nvidia-libs.mk b/make/rules-nvidia-libs.mk
new file mode 100644
index 0000000000000000000000000000000000000000..7526e9b4de2cd14025110553d504a3e79de36a03
--- /dev/null
+++ b/make/rules-nvidia-libs.mk
@@ -0,0 +1,41 @@
+# parameters:
+#   $(1): lowercase package name
+#   $(2): uppercase package name
+#   $(3): build target arch
+#   $(4): build target os
+#
+define create-rules-nvidia-libs
+$(call create-rules-common,$(1),$(2),$(3),$(4))
+ifneq ($(findstring $(3)-$(4),$(ARCHS)),)
+
+$(2)_MESON_ARGS = -Db_ndebug=true --buildtype=release --strip
+
+$(2)_$(3)_ENV += \
+	PATH="$$(WINE_$(3)_DST)/bin:$$$$PATH"
+
+$$($(2)_SRC)/meson.build: | $$(OBJ)/.$(1)-post-source
+
+$$(OBJ)/.$(1)-$(3)-configure: $$($(2)_SRC)/meson.build meson-source $$(OBJ)/.wine-$(3)-tools
+	@echo ":: configuring $(1)-$(3)..." >&2
+	rm -rf "$$($(2)_$(3)_OBJ)/meson-private/coredata.dat"
+
+	env $$($(2)_$(3)_ENV) \
+	$$(OBJ)/src-meson/meson.py "$$($(2)_$(3)_OBJ)" "$$($(2)_SRC)" \
+	      --prefix="$$($(2)_$(3)_DST)" \
+	      --buildtype=release \
+	      $$($(3)-$(4)_MESON_ARGS) \
+	      $$($(2)_MESON_ARGS) \
+	      $$($(2)_$(3)_MESON_ARGS) \
+	      $$(MESON_STRIP_ARG)
+
+	touch $$@
+
+$$(OBJ)/.$(1)-$(3)-build:
+	@echo ":: building $(1)-$(3)..." >&2
+	+env $$($(2)_$(3)_ENV) \
+	ninja -C "$$($(2)_$(3)_OBJ)" install
+	touch $$@
+endif
+endef
+
+rules-nvidia-libs = $(call create-rules-nvidia-libs,$(1),$(call toupper,$(1)),$(2),$(3))
diff --git a/make/rules-source.mk b/make/rules-source.mk
index 70352bac4025bade0a2a2782020b8fc92a647046..983ac3fec93b00df766e0da33fc204bf44dd373f 100644
--- a/make/rules-source.mk
+++ b/make/rules-source.mk
@@ -43,6 +43,7 @@ all: all-source
 $(1)-clean::
 $(1)-distclean::
 	rm -rf $$($(2)_SRC)
+	rm  -f $$(OBJ)/.$(1)-{source,post-source}
 
 clean: $(1)-clean
 distclean: $(1)-distclean
diff --git a/make/rules-wine-requests.mk b/make/rules-wine-requests.mk
index ce3caa5d15b934e2807ac13da5016b0d059363c2..1932245ffd2b55e33a21752e36cf19bf60914801 100644
--- a/make/rules-wine-requests.mk
+++ b/make/rules-wine-requests.mk
@@ -38,6 +38,11 @@ $$(OBJ)/.$(1)-wine-requests: | $$(OBJ)/.$(1)-post-source
 $$(OBJ)/.$(1)-i386-build: $$(OBJ)/.$(1)-wine-requests
 $$(OBJ)/.$(1)-x86_64-build: $$(OBJ)/.$(1)-wine-requests
 $$(OBJ)/.$(1)-aarch64-build: $$(OBJ)/.$(1)-wine-requests
+
+$(1)-requests-distclean::
+	rm -rf $$(OBJ)/.$(1)-wine-requests
+
+distclean: $(1)-requests-distclean
 endef
 
 rules-wine-requests = $(call create-rules-wine-requests,$(1),$(call toupper,$(1)))
diff --git a/make/rules-wine-tools.mk b/make/rules-wine-tools.mk
index 5f350fac024bf665e535b2c661bb2dd34abea041..60ffaa29485db4b759f2c6032da2b8c04a6b03d9 100644
--- a/make/rules-wine-tools.mk
+++ b/make/rules-wine-tools.mk
@@ -11,22 +11,20 @@ $$(OBJ)/.$(1)-$(3)-tools: CCACHE_BASEDIR = $$($(2)_SRC)
 $$(OBJ)/.$(1)-$(3)-tools: $$(OBJ)/.$(1)-source
 $$(OBJ)/.$(1)-$(3)-tools: $$(OBJ)/.$(1)-$(3)-configure
 
-ifneq ($(3),$$(HOST_ARCH))
-$$(OBJ)/.$(1)-$(3)-configure: | $$(OBJ)/.$(1)-$$(HOST_ARCH)-tools
-$$(OBJ)/.$(1)-$(3)-build: $$(OBJ)/.$(1)-$$(HOST_ARCH)-tools
-$(2)_$(3)_AUTOCONF_ARGS += --with-wine-tools=$$($(2)_$$(HOST_ARCH)_OBJ)
-endif
-
 $$(OBJ)/.$(1)-$(3)-tools:
-ifeq ($(3),$$(HOST_ARCH))
 	@echo ":: building $(1)-$(3)-tools..." >&2
 	+cd "$$($(2)_$(3)_OBJ)" && env $$($(2)_$(3)_ENV) \
 	$$(MAKE) __tooldeps__
-endif
+	+cd "$$($(2)_$(3)_OBJ)" && env $$($(2)_$(3)_ENV) \
+	$$(MAKE) nls/all
 	touch $$@
 
 $$(OBJ)/.$(1)-$(3)-build: $$(OBJ)/.$(1)-$(3)-tools
 
+$(1)-$(3)-tools-distclean::
+	rm -rf $$(OBJ)/.$(1)-$(3)-tools
+
+distclean: $(1)-$(3)-tools-distclean
 endif
 endef
 
diff --git a/meson b/meson
new file mode 160000
index 0000000000000000000000000000000000000000..53d4f7dc2dee749a973c9caa102473802644a386
--- /dev/null
+++ b/meson
@@ -0,0 +1 @@
+Subproject commit 53d4f7dc2dee749a973c9caa102473802644a386
diff --git a/nvidia-libs/build-dxvk-nvapi.mk b/nvidia-libs/build-dxvk-nvapi.mk
new file mode 100644
index 0000000000000000000000000000000000000000..646754ed84e5b524b384222e56f5f9b64811b666
--- /dev/null
+++ b/nvidia-libs/build-dxvk-nvapi.mk
@@ -0,0 +1,28 @@
+##
+## dxvk-nvapi
+##
+
+ifeq ($(findstring dxvk-nvapi,$(WITHOUT_NVIDIA_LIBS)),)
+
+NVLIBS_NVAPI_MESON_ARGS = -Db_ndebug=true
+NVLIBS_NVAPI_i386_MESON_ARGS = --bindir=$(NVLIBS_NVAPI_i386_DST)/lib/wine/nvidia-libs/nvapi/i386-windows
+NVLIBS_NVAPI_x86_64_MESON_ARGS = --bindir=$(NVLIBS_NVAPI_x86_64_DST)/lib/wine/nvidia-libs/nvapi/x86_64-windows
+
+NVLIBS_NVAPI_i386_CFLAGS = -O3 $(i386_SANITY_FLAGS)
+NVLIBS_NVAPI_x86_64_CFLAGS = -O3
+NVLIBS_NVAPI_CPPFLAGS = -msse -msse2
+NVLIBS_NVAPI_LDFLAGS = -static -static-libgcc -static-libstdc++
+
+$(eval $(call rules-source,nvlibs-nvapi,$(SRCDIR)/nvidia-libs/dxvk-nvapi))
+$(eval $(call rules-meson,nvlibs-nvapi,i386,windows))
+$(eval $(call rules-meson,nvlibs-nvapi,x86_64,windows))
+
+$(OBJ)/.nvlibs-nvapi-post-source:
+	mkdir -p $(DST_LIBDIR)/wine/nvidia-libs/nvapi
+	rm -rf $(DST_LIBDIR)/wine/nvidia-libs/nvapi/version
+	echo "$(shell git -C $(SRCDIR) submodule status -- nvidia-libs/dxvk-nvapi)" > $(DST_LIBDIR)/wine/nvidia-libs/nvapi/version
+	touch $@
+
+default_pfx: nvlibs-nvapi
+
+endif # WITHOUT_NVIDIA_LIBS
diff --git a/nvidia-libs/build-nvcuda.mk b/nvidia-libs/build-nvcuda.mk
new file mode 100644
index 0000000000000000000000000000000000000000..9944e3b878c504d9bd90d99d1f20c46b94987c98
--- /dev/null
+++ b/nvidia-libs/build-nvcuda.mk
@@ -0,0 +1,58 @@
+##
+## nvcuda32/nvcuda
+##
+
+ifeq ($(findstring nvcuda,$(WITHOUT_NVIDIA_LIBS)),)
+
+##
+## nvcuda32
+##
+
+ifneq ($(findstring i386,$(unix_ARCHS)),)
+
+NVCUDA32_i386_MESON_ARGS = \
+  --cross-file=$(NVCUDA32_SRC)/build-wine32.txt \
+  --libdir=lib/wine/nvidia-libs/nvcuda/i386-windows
+
+NVCUDA32_DEPENDS = wine
+NVCUDA32_HOST_DEPENDS = wine
+
+$(eval $(call rules-source,nvcuda32,$(SRCDIR)/nvidia-libs/nvcuda32))
+$(eval $(call rules-nvidia-libs,nvcuda32,i386,windows))
+
+$(OBJ)/.nvcuda32-post-source: $(OBJ)/.wine-wine-requests
+	$(foreach i,$(shell find $(NVCUDA32_SRC)/include/wine -type f | sort),cp $(WINE_SRC)/include/wine/$(notdir $(i)) $(i) &&) true
+	mkdir -p $(DST_LIBDIR)/wine/nvidia-libs/nvcuda
+	rm -rf $(DST_LIBDIR)/wine/nvidia-libs/nvcuda/version-i386
+	echo "$(shell git -C $(SRCDIR) submodule status -- nvidia-libs/nvcuda32)" > $(DST_LIBDIR)/wine/nvidia-libs/nvcuda/version-i386
+	touch $@
+
+default_pfx: nvcuda32
+
+endif # nvcuda32
+
+
+##
+## nvcuda
+##
+
+NVCUDA_x86_64_MESON_ARGS = \
+  --cross-file=$(NVCUDA_SRC)/build-wine64.txt \
+  --libdir=lib/wine/nvidia-libs/nvcuda/x86_64-windows
+
+NVCUDA_DEPENDS = wine
+NVCUDA_HOST_DEPENDS = wine
+
+$(eval $(call rules-source,nvcuda,$(SRCDIR)/nvidia-libs/nvcuda))
+$(eval $(call rules-nvidia-libs,nvcuda,x86_64,windows))
+
+$(OBJ)/.nvcuda-post-source: $(OBJ)/.wine-wine-requests
+	$(foreach i,$(shell find $(NVCUDA_SRC)/include/wine -type f | sort),cp $(WINE_SRC)/include/wine/$(notdir $(i)) $(i) &&) true
+	mkdir -p $(DST_LIBDIR)/wine/nvidia-libs/nvcuda
+	rm -rf $(DST_LIBDIR)/wine/nvidia-libs/nvcuda/version
+	echo "$(shell git -C $(SRCDIR) submodule status -- nvidia-libs/nvcuda)" > $(DST_LIBDIR)/wine/nvidia-libs/nvcuda/version
+	touch $@
+
+default_pfx: nvcuda
+
+endif # WITHOUT_NVIDIA_LIBS
diff --git a/nvidia-libs/build-nvenc.mk b/nvidia-libs/build-nvenc.mk
new file mode 100644
index 0000000000000000000000000000000000000000..f5d2f4009dd358bf9abd8c4057b15bfc7d43bec3
--- /dev/null
+++ b/nvidia-libs/build-nvenc.mk
@@ -0,0 +1,60 @@
+##
+## nvenc32/nvenc
+##
+
+ifeq ($(findstring nvenc,$(WITHOUT_NVIDIA_LIBS)),)
+
+##
+## nvenc32
+##
+
+ifneq ($(findstring i386,$(unix_ARCHS)),)
+
+NVENC32_i386_MESON_ARGS = \
+  --cross-file=$(NVENC32_SRC)/build-wine32.txt \
+  --libdir=lib/wine/nvidia-libs/nvenc/i386-windows
+
+NVENC32_DEPENDS = wine
+NVENC32_HOST_DEPENDS = wine
+
+$(eval $(call rules-source,nvenc32,$(SRCDIR)/nvidia-libs/nvenc32))
+$(eval $(call rules-nvidia-libs,nvenc32,i386,windows))
+
+$(OBJ)/.nvenc32-post-source: $(OBJ)/.wine-wine-requests
+	$(foreach i,$(shell find $(NVENC32_SRC)/include/wine -type f | sort),cp $(WINE_SRC)/include/wine/$(notdir $(i)) $(i) &&) true
+	mkdir -p $(DST_LIBDIR)/wine/nvidia-libs/nvenc
+	rm -rf $(DST_LIBDIR)/wine/nvidia-libs/nvenc/version-i386
+	echo "$(shell git -C $(SRCDIR) submodule status -- nvidia-libs/nvenc32)" > $(DST_LIBDIR)/wine/nvidia-libs/nvenc/version-i386
+	touch $@
+
+default_pfx: nvenc32
+
+endif # nvenc32
+
+
+##
+## nvenc
+##
+
+NVENC_x86_64_MESON_ARGS = \
+  --cross-file=$(NVENC_SRC)/build-wine64.txt \
+  --libdir=lib/wine/nvidia-libs/nvenc/x86_64-windows
+
+NVENC_DEPENDS = wine
+NVENC_HOST_DEPENDS = wine
+
+$(eval $(call rules-source,nvenc,$(SRCDIR)/nvidia-libs/nvenc))
+$(eval $(call rules-nvidia-libs,nvenc,x86_64,windows))
+
+
+$(OBJ)/.nvenc-post-source: $(OBJ)/.wine-wine-requests
+	$(foreach i,$(shell find $(NVENC_SRC)/include/wine -type f | sort),cp $(WINE_SRC)/include/wine/$(notdir $(i)) $(i) &&) true
+	mkdir -p $(DST_LIBDIR)/wine/nvidia-libs/nvenc
+	rm -rf $(DST_LIBDIR)/wine/nvidia-libs/nvenc/version
+	echo "$(shell git -C $(SRCDIR) submodule status -- nvidia-libs/nvenc)" > $(DST_LIBDIR)/wine/nvidia-libs/nvenc/version
+	touch $@
+
+
+default_pfx: nvenc
+
+endif # WITHOUT_NVIDIA_LIBS
diff --git a/nvidia-libs/build-wine-nvml.mk b/nvidia-libs/build-wine-nvml.mk
new file mode 100644
index 0000000000000000000000000000000000000000..5177cfac7c765263f000b73399143a7e452c128f
--- /dev/null
+++ b/nvidia-libs/build-wine-nvml.mk
@@ -0,0 +1,42 @@
+##
+## wine-nvml
+##
+
+ifeq ($(findstring nvml,$(WITHOUT_NVIDIA_LIBS)),)
+
+
+NVML_SO_x86_64_MESON_ARGS = \
+  --cross-file=$(NVML_SO_SRC)/cross-wine64.txt \
+  --libdir=lib/wine/nvidia-libs/nvml
+
+NVML_DLL_x86_64_MESON_ARGS = \
+  --cross-file=$(NVML_DLL_SRC)/cross-mingw64.txt \
+  --libdir=lib/wine/nvidia-libs/nvml
+
+NVML_SO_DEPENDS = wine
+NVML_SO_HOST_DEPENDS = wine
+NVML_DLL_DEPENDS = wine
+NVML_DLL_HOST_DEPENDS = wine
+
+$(eval $(call rules-source,nvml_so,$(SRCDIR)/nvidia-libs/wine-nvml))
+$(eval $(call rules-nvidia-libs,nvml_so,x86_64,unix))
+$(eval $(call rules-source,nvml_dll,$(SRCDIR)/nvidia-libs/wine-nvml))
+$(eval $(call rules-nvidia-libs,nvml_dll,x86_64,windows))
+
+$(OBJ)/.nvml_so-post-source:
+	cd $(NVML_SO_SRC)/src && ./make_nvml
+	mkdir -p $(DST_LIBDIR)/wine/nvidia-libs/nvml
+	rm -rf $(DST_LIBDIR)/wine/nvidia-libs/nvml/version
+	echo "$(shell git -C $(SRCDIR) submodule status -- nvidia-libs/wine-nvml)" > $(DST_LIBDIR)/wine/nvidia-libs/nvml/version
+	touch $@
+
+$(OBJ)/.nvml_dll-post-source:
+	cd $(NVML_DLL_SRC)/src && ./make_nvml
+	mkdir -p $(DST_LIBDIR)/wine/nvidia-libs/nvml
+	rm -rf $(DST_LIBDIR)/wine/nvidia-libs/nvml/version
+	echo "$(shell git -C $(SRCDIR) submodule status -- nvidia-libs/wine-nvml)" > $(DST_LIBDIR)/wine/nvidia-libs/nvml/version
+	touch $@
+
+default_pfx: nvml_so nvml_dll
+
+endif # WITHOUT_NVIDIA_LIBS
diff --git a/nvidia-libs/build-wine-nvoptix.mk b/nvidia-libs/build-wine-nvoptix.mk
new file mode 100644
index 0000000000000000000000000000000000000000..6421354c43c32db930c9999f861688af864cdf6e
--- /dev/null
+++ b/nvidia-libs/build-wine-nvoptix.mk
@@ -0,0 +1,26 @@
+##
+## wine-nvoptix
+##
+
+ifeq ($(findstring nvoptix,$(WITHOUT_NVIDIA_LIBS)),)
+
+
+NVOPTIX_x86_64_MESON_ARGS = \
+  --cross-file=$(NVOPTIX_SRC)/build-wine64.txt \
+  --libdir=lib/wine/nvidia-libs/nvoptix/x86_64-windows
+
+NVOPTIX_DEPENDS = wine
+NVOPTIX_HOST_DEPENDS = wine
+
+$(eval $(call rules-source,nvoptix,$(SRCDIR)/nvidia-libs/wine-nvoptix))
+$(eval $(call rules-nvidia-libs,nvoptix,x86_64,windows))
+
+$(OBJ)/.nvoptix-post-source:
+	mkdir -p $(DST_LIBDIR)/wine/nvidia-libs/nvoptix
+	rm -rf $(DST_LIBDIR)/wine/nvidia-libs/nvoptix/version
+	echo "$(shell git -C $(SRCDIR) submodule status -- nvidia-libs/wine-nvoptix)" > $(DST_LIBDIR)/wine/nvidia-libs/nvoptix/version
+	touch $@
+
+default_pfx: nvoptix
+
+endif # WITHOUT_NVIDIA_LIBS
diff --git a/nvidia-libs/dxvk-nvapi b/nvidia-libs/dxvk-nvapi
new file mode 160000
index 0000000000000000000000000000000000000000..ae0fcc09ee89b2ef47f4fb50ab3b597439bf2d5e
--- /dev/null
+++ b/nvidia-libs/dxvk-nvapi
@@ -0,0 +1 @@
+Subproject commit ae0fcc09ee89b2ef47f4fb50ab3b597439bf2d5e
diff --git a/nvidia-libs/nvcuda b/nvidia-libs/nvcuda
new file mode 160000
index 0000000000000000000000000000000000000000..772b344a3594fca916999c5c5288d875332de482
--- /dev/null
+++ b/nvidia-libs/nvcuda
@@ -0,0 +1 @@
+Subproject commit 772b344a3594fca916999c5c5288d875332de482
diff --git a/nvidia-libs/nvcuda32 b/nvidia-libs/nvcuda32
new file mode 160000
index 0000000000000000000000000000000000000000..1993a7c745e08f8bfb11502b7489dd7fba70a08f
--- /dev/null
+++ b/nvidia-libs/nvcuda32
@@ -0,0 +1 @@
+Subproject commit 1993a7c745e08f8bfb11502b7489dd7fba70a08f
diff --git a/nvidia-libs/nvenc b/nvidia-libs/nvenc
new file mode 160000
index 0000000000000000000000000000000000000000..09ec9e1c8b25a351e415a6d0361bfacad0fd710c
--- /dev/null
+++ b/nvidia-libs/nvenc
@@ -0,0 +1 @@
+Subproject commit 09ec9e1c8b25a351e415a6d0361bfacad0fd710c
diff --git a/nvidia-libs/nvenc32 b/nvidia-libs/nvenc32
new file mode 160000
index 0000000000000000000000000000000000000000..ff7220eb8a19ea7e0db8bd5c401a50b606712ff7
--- /dev/null
+++ b/nvidia-libs/nvenc32
@@ -0,0 +1 @@
+Subproject commit ff7220eb8a19ea7e0db8bd5c401a50b606712ff7
diff --git a/nvidia-libs/wine-nvml b/nvidia-libs/wine-nvml
new file mode 160000
index 0000000000000000000000000000000000000000..a1a7d8fac0e054122cbaf9205c9bee33f5831015
--- /dev/null
+++ b/nvidia-libs/wine-nvml
@@ -0,0 +1 @@
+Subproject commit a1a7d8fac0e054122cbaf9205c9bee33f5831015
diff --git a/nvidia-libs/wine-nvoptix b/nvidia-libs/wine-nvoptix
new file mode 160000
index 0000000000000000000000000000000000000000..ea2e23341ab84bcbebbf191f7f04cac13f1ac95b
--- /dev/null
+++ b/nvidia-libs/wine-nvoptix
@@ -0,0 +1 @@
+Subproject commit ea2e23341ab84bcbebbf191f7f04cac13f1ac95b
diff --git a/steamrtdeps/build-flac.mk b/steamrtdeps/build-flac.mk
new file mode 100644
index 0000000000000000000000000000000000000000..8bd148de10234f3c6ccc0cc384a7e38f3316013c
--- /dev/null
+++ b/steamrtdeps/build-flac.mk
@@ -0,0 +1,21 @@
+##
+## flac
+##
+
+FLAC_CONFIGURE_ARGS := \
+	--enable-shared \
+	--disable-static \
+	--disable-cpplibs \
+	--disable-programs \
+	--disable-examples \
+	--disable-doxygen-docs \
+	--disable-valgrind-testing
+
+$(eval $(call rules-source,flac,$(SRCDIR)/steamrtdeps/flac))
+$(eval $(call rules-configure,flac,i386,unix))
+
+$(OBJ)/.flac-post-source:
+	cd "$(FLAC_SRC)" && ./autogen.sh
+	touch $@
+
+FLAC_DEPENDENCY := flac
diff --git a/steamrtdeps/build-libpcap.mk b/steamrtdeps/build-libpcap.mk
new file mode 100644
index 0000000000000000000000000000000000000000..199b676cd06521ce51baf37f8d64882a64ed5997
--- /dev/null
+++ b/steamrtdeps/build-libpcap.mk
@@ -0,0 +1,25 @@
+##
+## libpcap
+##
+
+LIBPCAP_CONFIGURE_ARGS := \
+	--enable-shared \
+	--disable-static \
+	--disable-usb \
+	--disable-netmap \
+	--disable-bluetooth \
+	--disable-dbus \
+	--disable-rdma \
+	--with-pcap=linux \
+	--without-libnl
+
+$(eval $(call rules-source,libpcap,$(SRCDIR)/steamrtdeps/libpcap))
+$(eval $(call rules-configure,libpcap,i386,unix))
+$(eval $(call rules-configure,libpcap,x86_64,unix))
+$(eval $(call rules-configure,libpcap,aarch64,unix))
+
+$(OBJ)/.libpcap-post-source:
+	cd "$(LIBPCAP_SRC)" && autoreconf -fiv
+	touch $@
+
+LIBPCAP_DEPENDENCY := libpcap
diff --git a/steamrtdeps/build-libwebp.mk b/steamrtdeps/build-libwebp.mk
new file mode 100644
index 0000000000000000000000000000000000000000..965d75779df8c19f25320d5bb2f0bbc5d789a5e3
--- /dev/null
+++ b/steamrtdeps/build-libwebp.mk
@@ -0,0 +1,20 @@
+##
+## libwebp
+##
+
+LIBWEBP_CMAKE_ARGS := \
+	-DBUILD_SHARED_LIBS=ON \
+	-DWEBP_BUILD_ANIM_UTILS=OFF \
+	-DWEBP_BUILD_CWEBP=OFF \
+	-DWEBP_BUILD_DWEBP=OFF \
+	-DWEBP_BUILD_EXTRAS=OFF \
+	-DWEBP_BUILD_GIF2WEBP=OFF \
+	-DWEBP_BUILD_IMG2WEBP=OFF \
+	-DWEBP_BUILD_VWEBP=OFF \
+	-DWEBP_BUILD_WEBPINFO=OFF \
+	-DWEBP_BUILD_WEBPMUX=OFF \
+
+$(eval $(call rules-source,libwebp,$(SRCDIR)/steamrtdeps/libwebp))
+$(eval $(call rules-cmake,libwebp,i386,unix))
+
+LIBWEBP_DEPENDENCY := libwebp
diff --git a/steamrtdeps/build-libxkbcommon.mk b/steamrtdeps/build-libxkbcommon.mk
new file mode 100644
index 0000000000000000000000000000000000000000..11b9839b93e62dacfe976a5206ee7fe4a8885ccd
--- /dev/null
+++ b/steamrtdeps/build-libxkbcommon.mk
@@ -0,0 +1,19 @@
+##
+## xkbcommon
+##
+
+LIBXKBCOMMON_MESON_ARGS := \
+	-Denable-xkbregistry=true \
+	-Denable-docs=false \
+	-Denable-x11=false \
+	-Denable-bash-completion=false \
+	-Denable-wayland=false
+
+LIBXKBCOMMON_DEPENDS = libxml2
+
+$(eval $(call rules-source,libxkbcommon,$(SRCDIR)/steamrtdeps/libxkbcommon))
+$(eval $(call rules-meson,libxkbcommon,i386,unix))
+$(eval $(call rules-meson,libxkbcommon,x86_64,unix))
+$(eval $(call rules-meson,libxkbcommon,aarch64,unix))
+
+LIBXKBCOMMON_DEPENDENCY := libxkbcommon
diff --git a/steamrtdeps/build-libxml2.mk b/steamrtdeps/build-libxml2.mk
new file mode 100644
index 0000000000000000000000000000000000000000..ff62388b2c3ca2f0c5cc7ef5c64a19bc7442158b
--- /dev/null
+++ b/steamrtdeps/build-libxml2.mk
@@ -0,0 +1,16 @@
+##
+## libxml2
+##
+
+LIBXML2_MESON_ARGS := \
+	-Ddocs=disabled \
+	-Dicu=disabled \
+	-Dlegacy=enabled \
+	-Dpython=disabled
+
+$(eval $(call rules-source,libxml2,$(SRCDIR)/steamrtdeps/libxml2))
+$(eval $(call rules-meson,libxml2,i386,unix))
+$(eval $(call rules-meson,libxml2,x86_64,unix))
+$(eval $(call rules-meson,libxml2,aarch64,unix))
+
+LIBXML2_DEPENDENCY := libxml2
diff --git a/steamrtdeps/build-pipewire.mk b/steamrtdeps/build-pipewire.mk
new file mode 100644
index 0000000000000000000000000000000000000000..5175baeeadf24d20e971f47e19165165429acd1f
--- /dev/null
+++ b/steamrtdeps/build-pipewire.mk
@@ -0,0 +1,36 @@
+##
+## pipewire
+##
+
+PIPEWIRE_MESON_ARGS := \
+	-Davahi=disabled \
+	-Ddocs=disabled \
+	-Dexamples=disabled \
+	-Djack=disabled \
+	-Dman=disabled \
+	-Dpw-cat=disabled \
+	-Draop=disabled \
+	-Dsession-managers=[] \
+	-Dsnap=disabled \
+	-Dsystemd-user-service=disabled \
+	-Dtests=disabled \
+	-Dudev=disabled
+
+PIPEWIRE_i386_MESON_ARGS = \
+	-Dudevrulesdir=$(PIPEWIRE_i386_DST)/lib/udev/rules.d
+
+PIPEWIRE_DEPENDS = gstreamer gst_base
+
+$(eval $(call rules-source,pipewire,$(SRCDIR)/steamrtdeps/pipewire))
+$(eval $(call rules-meson,pipewire,i386,unix))
+
+$(OBJ)/.pipewire-i386-post-build:
+	rm -rf $(PIPEWIRE_i386_DST)/lib/udev
+	touch $@
+
+## Part of steamrt-libs:i386, use steamrt lib
+## Only use as build dependency; don't ship it.
+$(OBJ)/.pipewire-i386-dist:
+	touch $@
+
+PIPEWIRE_DEPENDENCY := pipewire
diff --git a/steamrtdeps/build-speex.mk b/steamrtdeps/build-speex.mk
new file mode 100644
index 0000000000000000000000000000000000000000..e348b7d51870c23f357bae1621f86942486e74d4
--- /dev/null
+++ b/steamrtdeps/build-speex.mk
@@ -0,0 +1,18 @@
+##
+## speex
+##
+
+SPEEX_CONFIGURE_ARGS := \
+	--enable-shared \
+	--disable-static \
+	--disable-binaries \
+	--disable-valgrind
+
+$(eval $(call rules-source,speex,$(SRCDIR)/steamrtdeps/speex))
+$(eval $(call rules-configure,speex,i386,unix))
+
+$(OBJ)/.speex-post-source:
+	cd "$(SPEEX_SRC)" && autoreconf -fiv
+	touch $@
+
+SPEEX_DEPENDENCY := speex
diff --git a/steamrtdeps/build-speexdsp.mk b/steamrtdeps/build-speexdsp.mk
new file mode 100644
index 0000000000000000000000000000000000000000..24cd4e831ae7df73522c6d7a6ab234c1d1c4eed2
--- /dev/null
+++ b/steamrtdeps/build-speexdsp.mk
@@ -0,0 +1,17 @@
+##
+## speexdsp
+##
+
+SPEEXDSP_CONFIGURE_ARGS := \
+	--enable-shared \
+	--disable-static \
+	--disable-examples
+
+$(eval $(call rules-source,speexdsp,$(SRCDIR)/steamrtdeps/speexdsp))
+$(eval $(call rules-configure,speexdsp,i386,unix))
+
+$(OBJ)/.speexdsp-post-source:
+	cd "$(SPEEXDSP_SRC)" && autoreconf -fiv
+	touch $@
+
+SPEEXDSP_DEPENDENCY := speexdsp
diff --git a/steamrtdeps/build-wayland-protocols.mk b/steamrtdeps/build-wayland-protocols.mk
new file mode 100644
index 0000000000000000000000000000000000000000..9e14d49a46bf5e92aa3954c7e1d3d834de783783
--- /dev/null
+++ b/steamrtdeps/build-wayland-protocols.mk
@@ -0,0 +1,13 @@
+##
+## wayland-protocols
+##
+
+WAYLAND_PROTOCOLS_MESON_ARGS := \
+	-Dtests=false
+
+$(eval $(call rules-source,wayland-protocols,$(SRCDIR)/steamrtdeps/wayland-protocols))
+$(eval $(call rules-meson,wayland-protocols,i386,unix))
+$(eval $(call rules-meson,wayland-protocols,x86_64,unix))
+$(eval $(call rules-meson,wayland-protocols,aarch64,unix))
+
+WAYLAND_PROTOCOLS_DEPENDENCY := wayland-protocols
diff --git a/steamrtdeps/build-xz.mk b/steamrtdeps/build-xz.mk
new file mode 100644
index 0000000000000000000000000000000000000000..ec1af26fd8371d436334ed89896786fd5976dcb9
--- /dev/null
+++ b/steamrtdeps/build-xz.mk
@@ -0,0 +1,25 @@
+##
+## xz
+##
+
+XZ_CONFIGURE_ARGS := \
+	--enable-shared \
+	--disable-static \
+	--disable-xz \
+	--disable-xzdec \
+	--disable-lzmadec \
+	--disable-lzmainfo \
+	--disable-lzma-links \
+	--disable-scripts \
+	--disable-doc \
+	--disable-doxygen \
+	--disable-nls
+
+$(eval $(call rules-source,xz,$(SRCDIR)/steamrtdeps/xz))
+$(eval $(call rules-configure,xz,i386,unix))
+
+$(OBJ)/.xz-post-source:
+	cd "$(XZ_SRC)" && autoreconf -fiv
+	touch $@
+
+XZ_DEPENDENCY := xz
diff --git a/steamrtdeps/flac b/steamrtdeps/flac
new file mode 160000
index 0000000000000000000000000000000000000000..1507800de4b70e21be71f38caa0d9079d0bc6e45
--- /dev/null
+++ b/steamrtdeps/flac
@@ -0,0 +1 @@
+Subproject commit 1507800de4b70e21be71f38caa0d9079d0bc6e45
diff --git a/steamrtdeps/libpcap b/steamrtdeps/libpcap
new file mode 160000
index 0000000000000000000000000000000000000000..a999701dca5c873779281938baee6bc185a8d4dc
--- /dev/null
+++ b/steamrtdeps/libpcap
@@ -0,0 +1 @@
+Subproject commit a999701dca5c873779281938baee6bc185a8d4dc
diff --git a/steamrtdeps/libwebp b/steamrtdeps/libwebp
new file mode 160000
index 0000000000000000000000000000000000000000..a4d7a715337ded4451fec90ff8ce79728e04126c
--- /dev/null
+++ b/steamrtdeps/libwebp
@@ -0,0 +1 @@
+Subproject commit a4d7a715337ded4451fec90ff8ce79728e04126c
diff --git a/steamrtdeps/libxkbcommon b/steamrtdeps/libxkbcommon
new file mode 160000
index 0000000000000000000000000000000000000000..dd642359f8d43c09968e34ca7f1eb1121b2dfd70
--- /dev/null
+++ b/steamrtdeps/libxkbcommon
@@ -0,0 +1 @@
+Subproject commit dd642359f8d43c09968e34ca7f1eb1121b2dfd70
diff --git a/steamrtdeps/libxml2 b/steamrtdeps/libxml2
new file mode 160000
index 0000000000000000000000000000000000000000..b7fa62cbe8ef0df5869e000d5b690bdedd07f33e
--- /dev/null
+++ b/steamrtdeps/libxml2
@@ -0,0 +1 @@
+Subproject commit b7fa62cbe8ef0df5869e000d5b690bdedd07f33e
diff --git a/steamrtdeps/pipewire b/steamrtdeps/pipewire
new file mode 160000
index 0000000000000000000000000000000000000000..d20a1523b6770dfa93a270bdda5d7c800d7ec191
--- /dev/null
+++ b/steamrtdeps/pipewire
@@ -0,0 +1 @@
+Subproject commit d20a1523b6770dfa93a270bdda5d7c800d7ec191
diff --git a/steamrtdeps/speex b/steamrtdeps/speex
new file mode 160000
index 0000000000000000000000000000000000000000..5dceaaf3e23ee7fd17c80cb5f02a838fd6c18e01
--- /dev/null
+++ b/steamrtdeps/speex
@@ -0,0 +1 @@
+Subproject commit 5dceaaf3e23ee7fd17c80cb5f02a838fd6c18e01
diff --git a/steamrtdeps/speexdsp b/steamrtdeps/speexdsp
new file mode 160000
index 0000000000000000000000000000000000000000..1b28a0f61bc31162979e1f26f3981fc3637095c8
--- /dev/null
+++ b/steamrtdeps/speexdsp
@@ -0,0 +1 @@
+Subproject commit 1b28a0f61bc31162979e1f26f3981fc3637095c8
diff --git a/steamrtdeps/wayland-protocols b/steamrtdeps/wayland-protocols
new file mode 160000
index 0000000000000000000000000000000000000000..810f1adaf33521cc55fc510566efba2a1418174f
--- /dev/null
+++ b/steamrtdeps/wayland-protocols
@@ -0,0 +1 @@
+Subproject commit 810f1adaf33521cc55fc510566efba2a1418174f
diff --git a/steamrtdeps/xz b/steamrtdeps/xz
new file mode 160000
index 0000000000000000000000000000000000000000..a522a226545730551f7e7c2685fab27cf567746c
--- /dev/null
+++ b/steamrtdeps/xz
@@ -0,0 +1 @@
+Subproject commit a522a226545730551f7e7c2685fab27cf567746c
diff --git a/vklayers/Vulkan-Utility-Libraries b/vklayers/Vulkan-Utility-Libraries
new file mode 160000
index 0000000000000000000000000000000000000000..b538fb5b08513aa78346cd414ad5e576a2a3e920
--- /dev/null
+++ b/vklayers/Vulkan-Utility-Libraries
@@ -0,0 +1 @@
+Subproject commit b538fb5b08513aa78346cd414ad5e576a2a3e920
diff --git a/vklayers/build-dxvk-nvapi-vkreflex-layer.mk b/vklayers/build-dxvk-nvapi-vkreflex-layer.mk
new file mode 100644
index 0000000000000000000000000000000000000000..90315cd958d9712355bfebc50073fb28d03477e8
--- /dev/null
+++ b/vklayers/build-dxvk-nvapi-vkreflex-layer.mk
@@ -0,0 +1,41 @@
+##
+## dxvk-nvapi-vkreflex-layer
+##
+
+ifeq ($(findstring dxvk-nvapi-vkreflex-layer,$(WITHOUT_VKLAYERS)),)
+
+DXVK_NVAPI_VKREFLEX_LAYER_SOURCE_ARGS = \
+  --exclude version.h.in \
+
+DXVK_NVAPI_VKREFLEX_LAYER_MESON_ARGS = -Dabsolute_library_path=false
+
+DXVK_NVAPI_VKREFLEX_LAYER_x86_64_CXXFLAGS = -std=c++20
+DXVK_NVAPI_VKREFLEX_LAYER_aarch64_CXXFLAGS = -std=c++20
+
+$(eval $(call rules-source,dxvk-nvapi-vkreflex-layer,$(SRCDIR)/dxvk-nvapi/layer))
+$(eval $(call rules-meson,dxvk-nvapi-vkreflex-layer,x86_64,unix))
+$(eval $(call rules-meson,dxvk-nvapi-vkreflex-layer,aarch64,unix))
+
+$(OBJ)/.dxvk-nvapi-vkreflex-layer-post-source: patches-source dxvk-nvapi-source
+	$(foreach p,$(shell find $(PATCHES_SRC)/vklayers/dxvk-nvapi-vkreflex-layer/ -name "*.patch" | sort),patch -d $(DXVK_NVAPI_VKREFLEX_LAYER_SRC) -Np2 -i $(p) &&) true
+	sed -re 's#@VCS_TAG@#$(shell git -C $(SRCDIR)/dxvk-nvapi describe --always --abbrev=15 --dirty=0)#' \
+	    $(SRCDIR)/dxvk-nvapi/version.h.in > $(DXVK_NVAPI_VKREFLEX_LAYER_SRC)/version.h.in
+	cp -a $(SRCDIR)/dxvk-nvapi/config.h.in $(DXVK_NVAPI_VKREFLEX_LAYER_SRC)/config.h.in
+	mkdir -p $(DXVK_NVAPI_VKREFLEX_LAYER_SRC)/external/
+	rsync -arx $(DXVK_NVAPI_SRC)/external/Vulkan-Headers/ $(DXVK_NVAPI_VKREFLEX_LAYER_SRC)/external/Vulkan-Headers/
+	rsync -arx $(DXVK_NVAPI_SRC)/external/vkroots/ $(DXVK_NVAPI_VKREFLEX_LAYER_SRC)/external/vkroots/
+	touch $@
+
+$(OBJ)/.dxvk-nvapi-vkreflex-layer-x86_64-post-build:
+	mkdir -p $(DST_DIR)/share/dxvk-nvapi-vkreflex-layer/implicit_layer.d/
+	cp -a $(DXVK_NVAPI_VKREFLEX_LAYER_x86_64_DST)/share/vulkan/implicit_layer.d/VkLayer_DXVK_NVAPI_reflex.json $(DST_DIR)/share/dxvk-nvapi-vkreflex-layer/implicit_layer.d/
+	touch $@
+
+$(OBJ)/.dxvk-nvapi-vkreflex-layer-aarch64-post-build:
+	mkdir -p $(DST_DIR)/share/dxvk-nvapi-vkreflex-layer/implicit_layer.d/
+	cp -a $(DXVK_NVAPI_VKREFLEX_LAYER_aarch64_DST)/share/vulkan/implicit_layer.d/VkLayer_DXVK_NVAPI_reflex.json $(DST_DIR)/share/dxvk-nvapi-vkreflex-layer/implicit_layer.d/
+	touch $@
+
+all-dist: dxvk-nvapi-vkreflex-layer
+
+endif # WITHOUT_VKLAYERS
diff --git a/vklayers/build-low_latency_layer.mk b/vklayers/build-low_latency_layer.mk
new file mode 100644
index 0000000000000000000000000000000000000000..d0f0f3a1e4b909f0bf5e032bfd2af974fa786d7b
--- /dev/null
+++ b/vklayers/build-low_latency_layer.mk
@@ -0,0 +1,34 @@
+##
+## low_latency_layer
+##
+
+ifeq ($(findstring low_latency_layer,$(WITHOUT_VKLAYERS)),)
+
+include $(SRC)/vklayers/build-vulkan-utility-libraries.mk
+
+LOW_LATENCY_LAYER_CMAKE_ARGS = \
+  -DCMAKE_BUILD_TYPE=release
+
+LOW_LATENCY_LAYER_DEPENDS = vulkan-headers vulkan-utility-libraries
+
+$(eval $(call rules-source,low_latency_layer,$(SRCDIR)/vklayers/low_latency_layer))
+$(eval $(call rules-cmake,low_latency_layer,x86_64,unix))
+$(eval $(call rules-cmake,low_latency_layer,aarch64,unix))
+
+$(OBJ)/.low_latency_layer-post-source: patches-source
+	$(foreach p,$(shell find $(PATCHES_SRC)/vklayers/low_latency_layer/ -name "*.patch" | sort),patch -d $(LOW_LATENCY_LAYER_SRC) -Np1 -i $(p) &&) true
+	touch $@
+
+$(OBJ)/.low_latency_layer-x86_64-post-build:
+	mkdir -p $(DST_DIR)/share/low_latency_layer/implicit_layer.d/
+	cp -a $(LOW_LATENCY_LAYER_x86_64_DST)/share/vulkan/implicit_layer.d/low_latency_layer.json $(DST_DIR)/share/low_latency_layer/implicit_layer.d/
+	touch $@
+
+$(OBJ)/.low_latency_layer-aarch64-post-build:
+	mkdir -p $(DST_DIR)/share/low_latency_layer/implicit_layer.d/
+	cp -a $(LOW_LATENCY_LAYER_aarch64_DST)/share/vulkan/implicit_layer.d/low_latency_layer.json $(DST_DIR)/share/low_latency_layer/implicit_layer.d/
+	touch $@
+
+all-dist: low_latency_layer
+
+endif # WITHOUT_VKLAYERS
diff --git a/vklayers/build-pyroveil.mk b/vklayers/build-pyroveil.mk
new file mode 100644
index 0000000000000000000000000000000000000000..b2a83fb868fc64f3080659ab8bb2ced0bda18ed8
--- /dev/null
+++ b/vklayers/build-pyroveil.mk
@@ -0,0 +1,34 @@
+##
+## pyroveil
+##
+
+ifeq ($(findstring pyroveil,$(WITHOUT_VKLAYERS)),)
+
+PYROVEIL_CMAKE_ARGS = \
+  -DCMAKE_BUILD_TYPE=release
+
+$(eval $(call rules-source,pyroveil,$(SRCDIR)/vklayers/pyroveil))
+$(eval $(call rules-cmake,pyroveil,x86_64,unix))
+$(eval $(call rules-cmake,pyroveil,aarch64,unix))
+
+$(OBJ)/.pyroveil-x86_64-post-build:
+	mkdir -p $(DST_DIR)/share/pyroveil/implicit_layer.d/
+	cat $(PYROVEIL_SRC)/layer/VkLayer_pyroveil.json.in | sed 's|@PYROVEIL_LAYER_PATH@|libVkLayer_pyroveil_64.so|g' > \
+		$(PYROVEIL_x86_64_DST)/share/vulkan/implicit_layer.d/VkLayer_pyroveil_64.json
+	cp -a $(PYROVEIL_x86_64_DST)/share/vulkan/implicit_layer.d/VkLayer_pyroveil_64.json $(DST_DIR)/share/pyroveil/implicit_layer.d/
+	mkdir -p $(DST_DIR)/share/pyroveil/
+	rsync --delete -arx $(PYROVEIL_SRC)/hacks $(DST_DIR)/share/pyroveil/
+	touch $@
+
+$(OBJ)/.pyroveil-aarch64-post-build:
+	mkdir -p $(DST_DIR)/share/pyroveil/implicit_layer.d/
+	cat $(PYROVEIL_SRC)/layer/VkLayer_pyroveil.json.in | sed 's|@PYROVEIL_LAYER_PATH@|libVkLayer_pyroveil_64.so|g' > \
+		$(PYROVEIL_aarch64_DST)/share/vulkan/implicit_layer.d/VkLayer_pyroveil_64.json
+	cp -a $(PYROVEIL_aarch64_DST)/share/vulkan/implicit_layer.d/VkLayer_pyroveil_64.json $(DST_DIR)/share/pyroveil/implicit_layer.d/
+	mkdir -p $(DST_DIR)/share/pyroveil/
+	rsync --delete -arx $(PYROVEIL_SRC)/hacks $(DST_DIR)/share/pyroveil/
+	touch $@
+
+all-dist: pyroveil
+
+endif # WITHOUT_VKLAYERS
diff --git a/vklayers/build-vkbasalt.mk b/vklayers/build-vkbasalt.mk
new file mode 100644
index 0000000000000000000000000000000000000000..4be29e73ab95490af9c2dcd9820aa8b5d05d5b1c
--- /dev/null
+++ b/vklayers/build-vkbasalt.mk
@@ -0,0 +1,35 @@
+##
+## vkbasalt
+##
+
+ifeq ($(findstring vkbasalt,$(WITHOUT_VKLAYERS)),)
+
+VKBASALT_MESON_ARGS = \
+  -Dwith_so=true \
+  -Dwith_json=true \
+  -Dappend_libdir_vkbasalt=false
+
+VKBASALT_DEPENDS = vulkan-headers spirv-headers
+
+$(eval $(call rules-source,vkbasalt,$(SRCDIR)/vklayers/vkbasalt))
+$(eval $(call rules-meson,vkbasalt,i386,unix))
+$(eval $(call rules-meson,vkbasalt,x86_64,unix))
+$(eval $(call rules-meson,vkbasalt,aarch64,unix))
+
+$(OBJ)/.vkbasalt-post-source: patches-source
+	$(foreach p,$(shell find $(PATCHES_SRC)/vklayers/vkbasalt/ -name "*.patch" | sort),patch -d $(VKBASALT_SRC) -Np1 -i $(p) &&) true
+	touch $@
+
+$(OBJ)/.vkbasalt-x86_64-post-build:
+	mkdir -p $(DST_DIR)/share/vkbasalt/implicit_layer.d/
+	cp -a $(VKBASALT_x86_64_DST)/share/vulkan/implicit_layer.d/vkBasalt.json $(DST_DIR)/share/vkbasalt/implicit_layer.d/
+	touch $@
+
+$(OBJ)/.vkbasalt-aarch64-post-build:
+	mkdir -p $(DST_DIR)/share/vkbasalt/implicit_layer.d/
+	cp -a $(VKBASALT_aarch64_DST)/share/vulkan/implicit_layer.d/vkBasalt.json $(DST_DIR)/share/vkbasalt/implicit_layer.d/
+	touch $@
+
+all-dist: vkbasalt
+
+endif # WITHOUT_VKLAYERS
diff --git a/vklayers/build-vulkan-utility-libraries.mk b/vklayers/build-vulkan-utility-libraries.mk
new file mode 100644
index 0000000000000000000000000000000000000000..1a74677e454796a02abe12708ab92ffd01c2a39d
--- /dev/null
+++ b/vklayers/build-vulkan-utility-libraries.mk
@@ -0,0 +1,18 @@
+##
+## vulkan-utility-libraries
+##
+
+VULKAN_UTILITY_LIBRARIES_CMAKE_ARGS = \
+  -DCMAKE_BUILD_TYPE=release
+
+VULKAN_UTILITY_LIBRARIES_DEPENDS = vulkan-headers
+
+$(eval $(call rules-source,vulkan-utility-libraries,$(SRCDIR)/vklayers/Vulkan-Utility-Libraries))
+$(eval $(call rules-cmake,vulkan-utility-libraries,x86_64,unix))
+$(eval $(call rules-cmake,vulkan-utility-libraries,aarch64,unix))
+
+## Only use vulkan-utility-libraries to build low_latency_layer; we don't ship it.
+$(OBJ)/.vulkan-utility-libraries-x86_64-dist:
+	touch $@
+$(OBJ)/.vulkan-utility-libraries-aarch64-dist:
+	touch $@
diff --git a/vklayers/low_latency_layer b/vklayers/low_latency_layer
new file mode 160000
index 0000000000000000000000000000000000000000..3138b14ebd059cd540444771dd184fbf7ead2a12
--- /dev/null
+++ b/vklayers/low_latency_layer
@@ -0,0 +1 @@
+Subproject commit 3138b14ebd059cd540444771dd184fbf7ead2a12
diff --git a/vklayers/pyroveil b/vklayers/pyroveil
new file mode 160000
index 0000000000000000000000000000000000000000..e1f547372cf1b9d14da56621716d2137088d0061
--- /dev/null
+++ b/vklayers/pyroveil
@@ -0,0 +1 @@
+Subproject commit e1f547372cf1b9d14da56621716d2137088d0061
diff --git a/vklayers/vkbasalt b/vklayers/vkbasalt
new file mode 160000
index 0000000000000000000000000000000000000000..4f97f09ffe91900e6ca136cc26cf7966f8f6970d
--- /dev/null
+++ b/vklayers/vkbasalt
@@ -0,0 +1 @@
+Subproject commit 4f97f09ffe91900e6ca136cc26cf7966f8f6970d
